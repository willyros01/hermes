name: FIDUNIO 0.9.6.25 docs
on:
  push:
    branches: [fidunio-complete-rebuild]
    paths: ['.fidunio-09625-docs-trigger']
permissions:
  contents: write
jobs:
  reconcile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: fidunio-complete-rebuild}
      - run: python .fidunio-09625-docs.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add README.md hermes-memory.txt FIDUNIO-BUILD-CHECKLIST.md CURRENT-REBUILD.md DISAPPEARING-PURGE-AUTHORITY.md architecture-ownership.txt RUNTIME-AUTHORITY-MAP.md
          git diff --cached --quiet || git commit -m "Reconcile FIDUNIO 0.9.6.25 documentation"
          git push
