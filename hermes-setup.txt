HERMES / FIDUNIO 0.8.1.8 — COMPLETE TABLET SCREEN ALIGNMENT

PURPOSE
UI-only patch completing the responsive tablet treatment.

CHANGES
- Settings screen now uses a centered tablet-width workspace.
- First/unlock screen now uses tablet-aware width and centering.
- New Message/New Group/Group Name tablet treatment from 0.8.1.7 remains.
- Two-pane iPad chat remains.
- One-line centered attachment toolbar remains.
- iPhone/narrow screens remain unchanged.

NO SECURITY/BACKEND CHANGES
No Firebase, Firestore Rules, E2EE, contact-key verification, device identity, Outbox, local history, or reconnect changes.

GITHUB
1. Upload ZIP contents to the ROOT of the hermes repository.
2. Preserve firebase-config.js and config-firestore.js.
3. Overwrite hermes-memory.txt and hermes-setup.txt.
4. Confirm version.js shows 0.8.1.8.
5. No Firestore Rules publication is required.

TEST
1. Launch on iPad:
   PASS = first/unlock screen is centered and tablet-sized, not a narrow phone column.
2. Open Settings:
   PASS = Settings uses the same centered tablet-width presentation.
3. Open New Message / New Group:
   PASS = tablet-width treatment remains.
4. Return to Messages:
   PASS = two-pane layout remains.
5. Open + tools:
   PASS = one centered horizontal row of 8.
6. iPhone:
   PASS = existing single-pane layout remains unchanged.
7. Send one short message each direction.
