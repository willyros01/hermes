HERMES / FIDUNIO 0.8.1.2 — RESPONSIVE 2D ICON FIX

PURPOSE
UI-only correction to 0.8.1.1.

CHANGES
- Fixes attachment/tool-panel overflow on iPad.
- iPad/tablet: 4-column tool grid.
- iPhone/narrow screens: 2-column tool grid so every button remains fully visible.
- Adds distinct color accents to Photo, File, Voice, Location, Contact, Checklist, Schedule, and Saved icons.
- Quick-reply row can scroll horizontally on narrow screens instead of forcing layout overflow.

NO FUNCTIONAL/SECURITY CHANGES
No changes to Firebase, Firestore rules, E2EE, key verification, device identity, Outbox, local history, or reconnect behavior.

GITHUB
1. Upload ZIP contents to the ROOT of the hermes repository.
2. Preserve firebase-config.js and config-firestore.js.
3. Overwrite hermes-memory.txt and hermes-setup.txt.
4. Confirm version.js says 0.8.1.2.
5. No Firestore rules update is required.

TEST
- iPad: open + panel; all 8 tools should fit inside the app width with 4 columns.
- iPhone: open + panel; all 8 tools should fit with 2 columns and no right-side clipping.
- Confirm colored icons are easy to distinguish.
- Send one message each direction to confirm no regression.
