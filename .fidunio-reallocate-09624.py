from pathlib import Path
p=Path('FIDUNIO-BUILD-CHECKLIST.md');s=p.read_text()
s=s.replace('| **0.9.6.22–0.9.6.25** | IndexedDB/Outbox purge wiring, authoritative projection convergence, restart/reconnect proof, multi-device expiry convergence | **4.0** (1.0 each) |\n| **0.9.6.26–0.9.6.27** | Disappearing-text UI + end-to-end security closeout | **2.0** (1.0 each) |\n| **0.9.6.28–0.9.6.29** | Group earlier-history UI + receipt/lifecycle stabilization | **2.0** (1.0 each) |','| **0.9.6.22–0.9.6.23** | IndexedDB/Outbox purge wiring + authoritative projection convergence | **2.0** (1.0 each) |\n| **0.9.6.24** | Mandatory bounded group-purge transaction write limit | **0.5** |\n| **0.9.6.25** | Restart/reconnect stale-client anti-resurrection proof | **0.5** |\n| **0.9.6.26** | Multi-device expiry convergence | **1.0** |\n| **0.9.6.27–0.9.6.28** | Disappearing-text UI + end-to-end security closeout | **2.0** (1.0 each) |\n| **0.9.6.29–0.9.6.30** | Group earlier-history UI + receipt/lifecycle stabilization | **2.0** (1.0 each) |')
old='''| **0.9.6.24** | **Restart/reconnect stale-client anti-resurrection proof.** Persisted history + encrypted Outbox survive ordinary offline restart, but an already-authoritatively-purged disappearing ID is removed before reconnect flush/projection. Prove stale cache cannot re-upload or re-project expired material. | Cold-start, offline->online, stale snapshot and duplicate retry tests green; no `location.reload()`/timer rescue. | PLANNED |\n| **0.9.6.25** | **Multi-device expiry convergence foundation.** Define and test same-UID device behavior after server purge; each device independently converges local traces to authoritative absence without installation-local lifetime authority. | Repository multi-device simulation proves both devices converge and neither can resurrect source/Outbox/history. | PLANNED |\n| **0.9.6.26** | **Disappearing text settings/UI wiring.** Expose user-selected duration for direct/group text messages using existing immutable outer `disappearAfterSeconds`; show effective policy before send; do not redesign established chat layout. | UI/controller tests prove selected duration reaches direct/group send paths and already-sent message duration remains immutable. | PLANNED |\n| **0.9.6.27** | **Disappearing text end-to-end security closeout.** Reconcile direct/group text purge, receipts, history grants, local cache, Outbox, stale clients and multi-device behavior as one release checkpoint. | Complete disappearing-text repository matrix green; checklist text-message items can move to DONE except attachment-specific work. | PLANNED |\n| **0.9.6.28** | **Group earlier-history admin UI.** Enable Group Info date/beginning grant controls only against already-validated server-backed history source selection and grant runtime; preserve admin-only intent and from-join default. | UI integration + group history rules/runtime/projection tests green; no cache-only grant source. | PLANNED |\n| **0.9.6.29** | **Receipt/lifecycle stabilization before attachment phase.** Close remaining iPad/two-pane live Sent->Read refresh behavior and verify direct/group receipt projection does not regress under disappearance handling. | Repository lifecycle tests green; ready for real-device proof later in 0.9.9.x. | PLANNED |'''
new='''| **0.9.6.24** | **CURRENT — bounded group-purge transaction write limit.** Close the known 0.9.6.20 omission before further anti-resurrection work: compute every receipt delete, source-linked history-copy delete, empty-grant delete, retained-grant update and final source delete before scheduling transaction writes; fail closed above one conservative repository maximum with `PURGE_TRACE_TOO_LARGE`. No chunked source-first deletion, tombstone, or client delete authority. | Exact-boundary commit succeeds; boundary+1 throws `PURGE_TRACE_TOO_LARGE` before any transaction write is scheduled; unrelated traces remain untouched; full baseline gate green. | CURRENT |\n| **0.9.6.25** | **Restart/reconnect stale-client anti-resurrection proof.** Persisted history + encrypted Outbox survive ordinary offline restart, but an already-authoritatively-purged disappearing ID is removed before reconnect flush/projection. Prove stale cache cannot re-upload or re-project expired material. | Cold-start, offline->online, stale snapshot and duplicate retry tests green; no `location.reload()`/timer rescue. | PLANNED |\n| **0.9.6.26** | **Multi-device expiry convergence foundation.** Define and test same-UID device behavior after server purge; each device independently converges local traces to authoritative absence without installation-local lifetime authority. | Repository multi-device simulation proves both devices converge and neither can resurrect source/Outbox/history. | PLANNED |\n| **0.9.6.27** | **Disappearing text settings/UI wiring.** Expose user-selected duration for direct/group text messages using existing immutable outer `disappearAfterSeconds`; show effective policy before send; do not redesign established chat layout. | UI/controller tests prove selected duration reaches direct/group send paths and already-sent message duration remains immutable. | PLANNED |\n| **0.9.6.28** | **Disappearing text end-to-end security closeout.** Reconcile direct/group text purge, receipts, history grants, local cache, Outbox, stale clients and multi-device behavior as one release checkpoint. | Complete disappearing-text repository matrix green; checklist text-message items can move to DONE except attachment-specific work. | PLANNED |\n| **0.9.6.29** | **Group earlier-history admin UI.** Enable Group Info date/beginning grant controls only against already-validated server-backed history source selection and grant runtime; preserve admin-only intent and from-join default. | UI integration + group history rules/runtime/projection tests green; no cache-only grant source. | PLANNED |\n| **0.9.6.30** | **Receipt/lifecycle stabilization before attachment phase.** Close remaining iPad/two-pane live Sent->Read refresh behavior and verify direct/group receipt projection does not regress under disappearance handling. | Repository lifecycle tests green; ready for real-device proof later in 0.9.9.x. | PLANNED |'''
if old not in s: raise SystemExit('roadmap anchor missing')
s=s.replace(old,new)
p.write_text(s)
note='''\n\n### Mandatory build reallocation after 0.9.6.23\nRepository review reconfirmed the known 0.9.6.20 omission: group physical purge still lacks an explicit conservative transaction write-count ceiling. Per the pre-allocation rule, this security repair is allocated **before implementation** as 0.9.6.24. The previous 0.9.6.24–0.9.6.29 work shifts to 0.9.6.25–0.9.6.30. The weighted denominator remains 100: 0.9.6.24 receives 0.5 point and restart/reconnect 0.9.6.25 receives 0.5 point; later shifted builds retain their prior points. Current earned completion remains 69%.\n'''
for name in ['hermes-memory.txt','CURRENT-REBUILD.md','README.md']:
    q=Path(name);t=q.read_text();marker='### Mandatory build reallocation after 0.9.6.23'
    if marker not in t:q.write_text(t.rstrip()+note+'\n')
print('0.9.6.24 security build allocated before implementation')
