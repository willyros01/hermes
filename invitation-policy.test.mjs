import {assertInvitationUsable,normalizeInvitationRole,canIssueInvitation} from './invitation-policy.js';let bad=0;const ok=(n,v)=>{console.log(v?'PASS':'FAIL',n);if(!v)bad++};const fail=(n,fn,part)=>{try{fn();ok(n,false)}catch(e){ok(n,String(e.message).includes(part))}};ok('pending unexpired invitation usable',assertInvitationUsable({status:'pending',expiresAt:new Date(2000),now:1000}));fail('used invitation rejected',()=>assertInvitationUsable({status:'accepted',expiresAt:new Date(2000),now:1000}),'already been used');fail('revoked invitation rejected',()=>assertInvitationUsable({status:'revoked',expiresAt:new Date(2000),now:1000}),'no longer active');fail('expired invitation rejected',()=>assertInvitationUsable({status:'pending',expiresAt:new Date(999),now:1000}),'expired');ok('user/admin roles allowed',normalizeInvitationRole('user')==='user'&&normalizeInvitationRole('admin')==='admin');fail('invalid role rejected',()=>normalizeInvitationRole('owner'),'User or Admin');ok('only owner/admin can issue',canIssueInvitation('owner')&&canIssueInvitation('admin')&&!canIssueInvitation('user'));if(bad)process.exitCode=1;
