HERMES 0.6.2 / FIDUNIO 0.6.2 — TEST

Upload every file in this ZIP to the ROOT of the existing hermes repo.
Do NOT delete or overwrite the existing firebase-config.js.
Also preserve config-firestore.js if present.
No Firebase Console changes are required.

After deployment, confirm Version 0.6.2 on both iPad and iPhone.

TEST A — LIVE FOREGROUND RECEIVE
1. Keep both devices online.
2. Open the existing cloud conversation on BOTH devices.
3. Do not kill either app.
4. iPad sends: Live 0.6.2 from iPad
5. PASS: it appears on iPhone immediately without killing/reopening.
6. iPhone sends: Live 0.6.2 from iPhone
7. PASS: it appears on iPad immediately without killing/reopening.

If Test A fails, stop and report the result.

TEST B — OFFLINE CACHE
Only after Test A passes:
1. On iPad, with the new messages visible, turn Airplane Mode ON.
2. Kill FIDUNIO.
3. Reopen while still offline.
4. PASS: previously downloaded messages remain visible.

TEST C — OUTBOX SURVIVAL
1. Still offline, send: Offline queued 0.6.2
2. Confirm Queued.
3. Kill and reopen FIDUNIO while still offline.
4. PASS: the queued message is still present.

TEST D — RECONNECT
1. Turn Airplane Mode OFF.
2. Foreground FIDUNIO.
3. PASS: Offline queued 0.6.2 reaches the iPhone and remains visible on iPad.

Use harmless test messages only; 0.6.2 is still not end-to-end encrypted.
