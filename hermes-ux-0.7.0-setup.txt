HERMES 0.7.0 / FIDUNIO 0.7.0 — E2EE TEST

IMPORTANT: publish the INCLUDED firestore.rules before encrypted sending.

1. Upload all ZIP files to the ROOT of the existing hermes repo.
2. Do NOT delete/overwrite firebase-config.js or config-firestore.js; neither is in the ZIP.
3. Firebase Console > fidunio-fef13 > Firestore Database > Rules:
   replace current rules with included firestore.rules and Publish.
4. Launch FIDUNIO 0.7.0 ONLINE on BOTH iPad and iPhone and leave each open several seconds.
   Each installation generates its private device key locally and publishes only its public key.
5. Existing direct conversation:
   iPad sends: Encrypted 0.7.0 from iPad
   iPhone sends: Encrypted 0.7.0 from iPhone
   Both must arrive readable.
6. If "[Encrypted message — key unavailable]" appears, STOP and report it.
7. Offline regression: Airplane Mode on iPad, kill/reopen, verify encrypted messages remain readable.
8. Still offline send: Encrypted queued 0.7.0. Kill/reopen; verify Queued survives.
9. Restore comms; verify it reaches iPhone readable.

For NEW 0.7.0 direct messages Firestore should contain e2ee=1, ciphertext, iv, text="" plus metadata.

LIMITATIONS: first E2EE foundation only. No multi-device key fan-out, safety-number/QR verification,
key-change warning, forward secrecy/Double Ratchet, or group E2EE yet. Metadata remains visible.
Old pre-0.7.0 messages remain legacy plaintext. Do not use sensitive material yet.
