/* FIDUNIO system administration: compact account lifecycle + invitation revoke. */
const SDK_VERSION="12.18.0";
let mountedForUid="";
let apiPromise=null;
function esc(s=""){return String(s??"").replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function api(){if(apiPromise)return apiPromise;apiPromise=Promise.all([import(`https://www.gstatic.com/firebasejs/${SDK_VERSION}/firebase-app.js`),import(`https://www.gstatic.com/firebasejs/${SDK_VERSION}/firebase-auth.js`),import(`https://www.gstatic.com/firebasejs/${SDK_VERSION}/firebase-firestore.js`)]).then(([appSdk,authSdk,fsSdk])=>{const app=appSdk.getApp();return{auth:authSdk.getAuth(app),db:fsSdk.getFirestore(app),authSdk,fsSdk};});return apiPromise;}
function profileStatus(p){if(p?.active===false)return p.status||"suspended";return p?.status||"active";}
function dateText(ts){const d=ts?.toDate?.();return d?d.toLocaleDateString():"No expiration";}
function initials(name){return String(name||"U").split(/\s+/).slice(0,2).map(x=>x[0]).join("").toUpperCase();}
async function currentAccess(){const s=await api(),u=s.auth.currentUser;if(!u)return null;const snap=await s.fsSdk.getDoc(s.fsSdk.doc(s.db,"users",u.uid));if(!snap.exists())return null;return{user:u,profile:{uid:snap.id,...snap.data()}};}
async function listUsers(){const s=await api(),snap=await s.fsSdk.getDocs(s.fsSdk.collection(s.db,"users"));return snap.docs.map(d=>({uid:d.id,...d.data()})).sort((a,b)=>String(a.displayName||a.email||a.uid).localeCompare(String(b.displayName||b.email||b.uid)));}
async function listInvites(){const s=await api(),snap=await s.fsSdk.getDocs(s.fsSdk.collection(s.db,"invitations"));return snap.docs.map(d=>({id:d.id,...d.data()})).sort((a,b)=>(b.createdAt?.seconds||0)-(a.createdAt?.seconds||0));}
async function updateLifecycle(target,status,expiryDays=undefined){const s=await api(),me=s.auth.currentUser;if(!me)throw new Error("Sign in first.");const now=s.fsSdk.serverTimestamp(),ref=s.fsSdk.doc(s.db,"users",target.uid),row={status,active:status==="active",adminUpdatedAt:now,adminUpdatedByUid:me.uid};if(expiryDays!==undefined)row.expiresAt=expiryDays===null?null:s.fsSdk.Timestamp.fromDate(new Date(Date.now()+Number(expiryDays)*86400000));if(status==="suspended"){row.suspendedAt=now;row.suspendedByUid=me.uid;row.restoredAt=null;row.restoredByUid=null;}if(status==="active"){row.restoredAt=now;row.restoredByUid=me.uid;row.suspendedAt=null;row.suspendedByUid=null;row.deactivatedAt=null;row.deactivatedByUid=null;}if(status==="deactivated"){row.deactivatedAt=now;row.deactivatedByUid=me.uid;}await s.fsSdk.updateDoc(ref,row);}
async function setExpiration(target,days){await updateLifecycle(target,profileStatus(target),days==="never"?null:Number(days));}
async function revokeInvitation(invite){const s=await api();await s.fsSdk.updateDoc(s.fsSdk.doc(s.db,"invitations",invite.id),{status:"revoked",revokedAt:s.fsSdk.serverTimestamp()});}
function closeModal(){document.querySelector("#fidunioAdminModal")?.remove();}
function closeMenus(host){host.querySelectorAll(".admin-menu.open").forEach(m=>m.classList.remove("open"));}
function canManageUser(u,access){const role=access.profile.systemRole,isSelf=u.uid===access.user.uid;return !isSelf&&u.systemRole!=="owner"&&(u.systemRole!=="admin"||role==="owner");}
function statusBadge(status){return `<span class="admin-status admin-status-${esc(status)}">${esc(status.toUpperCase())}</span>`;}
function userRow(u,access){const status=profileStatus(u),manageable=canManageUser(u,access),name=u.displayName||u.email||"FIDUNIO user";return `<div class="admin-user-row" data-uid="${esc(u.uid)}"><div class="admin-user-person"><div class="admin-avatar">${esc(initials(name))}</div><div><strong>${esc(name)}</strong><span>${esc(u.email||"")}</span></div></div><div class="admin-role">${esc((u.systemRole||"user").toUpperCase())}</div><div>${statusBadge(status)}</div><div class="admin-expiry">${esc(u.expiresAt?dateText(u.expiresAt):"No expiration")}</div><div class="admin-actions-cell">${manageable?`<button class="admin-more" type="button" data-uid="${esc(u.uid)}" aria-label="Actions for ${esc(name)}">•••</button><div class="admin-menu" data-menu-uid="${esc(u.uid)}"><button data-admin-action="${status==="active"?"suspended":"active"}">${status==="active"?"Suspend":"Restore"}</button><button data-admin-action="deactivated">Deactivate</button><div class="admin-menu-sep"></div><span>Expiration</span><button data-expiry="never">Never</button><button data-expiry="7">7 days</button><button data-expiry="30">30 days</button><button data-expiry="90">90 days</button></div>`:`<span class="admin-protected">${u.uid===access.user.uid?"Current":u.systemRole==="owner"?"Protected":"Owner only"}</span>`}</div></div>`;}
async function renderAdmin(host){const access=await currentAccess();if(!access||!["owner","admin"].includes(access.profile.systemRole)){closeModal();return;}const [users,invites]=await Promise.all([listUsers(),listInvites()]);const pending=invites.filter(i=>i.status==="pending");host.querySelector(".modal").innerHTML=`<div class="admin-modal-head"><div><h2>User Administration</h2><p class="small-note">Manage user access, status, and expiration without deleting message or group history.</p></div><button class="secondary" id="adminRefreshBtn" style="width:auto">Refresh</button></div><div class="admin-section-label">Users (${users.length})</div><div class="admin-user-table"><div class="admin-user-header"><span>User</span><span>Role</span><span>Status</span><span>Expires</span><span></span></div>${users.map(u=>userRow(u,access)).join("")}</div><p class="small-note" style="margin-top:10px">Suspended, deactivated, or expired users are blocked by Firestore access rules.</p><div class="admin-section-label" style="margin-top:16px">Pending Invitations (${pending.length})</div><div class="admin-invite-list">${pending.length?pending.map(i=>`<div class="admin-invite-row"><div><strong>${esc((i.role||"user").toUpperCase())}</strong><span>${esc(i.invitedByName||"Administrator")} • ${esc(dateText(i.expiresAt))}</span></div><button class="row-action revokeInviteBtn" data-id="${esc(i.id)}">Revoke</button></div>`).join(""):'<p class="small-note">No pending invitations.</p>'}</div><div class="modal-actions"><button class="modal-cancel" id="adminCloseBtn">Close</button></div>`;host.querySelector("#adminCloseBtn").onclick=closeModal;host.querySelector("#adminRefreshBtn").onclick=()=>renderAdmin(host);host.querySelectorAll(".admin-more").forEach(btn=>btn.onclick=e=>{e.stopPropagation();const menu=host.querySelector(`.admin-menu[data-menu-uid="${CSS.escape(btn.dataset.uid)}"]`),open=menu.classList.contains("open");closeMenus(host);if(!open)menu.classList.add("open");});host.querySelectorAll(".admin-menu button[data-admin-action]").forEach(btn=>btn.onclick=async e=>{e.stopPropagation();const menu=btn.closest(".admin-menu"),u=users.find(x=>x.uid===menu.dataset.menuUid),action=btn.dataset.adminAction;if(action==="deactivated"&&!confirm(`Deactivate ${u.displayName||u.email||"this account"}?`))return;btn.disabled=true;try{await updateLifecycle(u,action);await renderAdmin(host);}catch(err){alert(err?.message||String(err));btn.disabled=false;}});host.querySelectorAll(".admin-menu button[data-expiry]").forEach(btn=>btn.onclick=async e=>{e.stopPropagation();const menu=btn.closest(".admin-menu"),u=users.find(x=>x.uid===menu.dataset.menuUid);btn.disabled=true;try{await setExpiration(u,btn.dataset.expiry);await renderAdmin(host);}catch(err){alert(err?.message||String(err));btn.disabled=false;}});host.querySelectorAll(".revokeInviteBtn").forEach(btn=>btn.onclick=async()=>{const i=invites.find(x=>x.id===btn.dataset.id);btn.disabled=true;try{await revokeInvitation(i);await renderAdmin(host);}catch(err){alert(err?.message||String(err));btn.disabled=false;}});}
async function openAdmin(){if((globalThis.FIDUNIO_INVITE_FLOW_ACTIVE_UNTIL||0)>Date.now())return;const access=await currentAccess();if(!access||!["owner","admin"].includes(access.profile.systemRole))return;closeModal();const host=document.createElement("div");host.id="fidunioAdminModal";host.className="modal-backdrop";host.innerHTML='<div class="modal fidunio-admin-modal"><h2>User Administration</h2><p class="small-note">Loading users…</p></div>';document.body.appendChild(host);host.onclick=e=>{if(e.target===host)closeModal();else if(!e.target.closest(".admin-more,.admin-menu"))closeMenus(host);};try{await renderAdmin(host);}catch(err){host.querySelector(".modal").innerHTML=`<h2>User Administration</h2><p class="warning-note">${esc(err?.message||String(err))}</p><button class="secondary" id="adminCloseBtn">Close</button>`;host.querySelector("#adminCloseBtn").onclick=closeModal;}}
async function mount(){const settings=document.querySelector(".content.settings");if(!settings)return;const access=await currentAccess().catch(()=>null);if(!access||!["owner","admin"].includes(access.profile.systemRole)){document.querySelector("#fidunioUserAdminCard")?.remove();return;}if(document.querySelector("#fidunioUserAdminCard"))return;const card=document.createElement("div");card.className="card";card.id="fidunioUserAdminCard";card.innerHTML='<h2>User Administration</h2><p class="small-note">Compact user list for access, suspension, restoration, expiration, and pending invitations.</p><button class="primary" type="button" id="manageUsersBtn">Manage Users & Access</button>';settings.appendChild(card);const btn=card.querySelector("#manageUsersBtn");btn.disabled=false;btn.onclick=e=>{e.preventDefault();e.stopPropagation();openAdmin();};mountedForUid=access.user.uid;}
let queued=false;function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;mount();});}const observer=new MutationObserver(schedule);observer.observe(document.documentElement,{subtree:true,childList:true});mount();
