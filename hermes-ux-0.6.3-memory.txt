HERMES / FIDUNIO PROJECT MEMORY — 0.6.3

Hermes = internal project/repo/ZIP. FIDUNIO — Private Messaging = public product.
Repo remains hermes, flat root files.

Protected config:
- config-firestore.js: never overwrite/regenerate/include in release ZIP.
- firebase-config.js: preserve configured GitHub copy; omitted from 0.6.3.

Passed:
- 0.6 real Firebase two-account/two-device transport.
- 0.6.2 live foreground messaging iPad<->iPhone works without killing/reopening.

Failed offline history test on 0.6.2:
- iPad online messages were visible.
- Airplane Mode on, kill/reopen.
- cloud conversation had no iPhone/cloud messages.
- header still said Connected even offline.

0.6.3 fix:
- IndexedDB v2 adds dedicated encrypted `history` object store.
- Firestore snapshots are saved per conversation to that independent history cache.
- Offline startup restores history before Outbox and first render.
- Pending local messages are merged into cached history.
- Safari/iOS local AES key creation uses separate read and write IndexedDB transactions; no transaction is held across async Web Crypto generation.
- Header says Cloud, not Connected, so it no longer falsely claims network reachability.
- Retains 0.6.1 authoritative encrypted Outbox fixes.
- Retains 0.6.2 foreground listener/auth/resume fixes.

Test order:
1. Online baseline.
2. Offline history cold start. Stop here if it fails.
3. Offline queued-message survival.
4. Reconnect delivery.

Security: cloud content still plaintext in Firestore; no E2EE yet. Harmless testing only.
