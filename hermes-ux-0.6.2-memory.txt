HERMES / FIDUNIO PROJECT MEMORY — 0.6.2

Hermes is the internal project/repo/ZIP name. FIDUNIO — Private Messaging is the public product name. Repo remains hermes and releases stay flat in repository root.

Protected configuration:
- config-firestore.js must never be overwritten/regenerated or included in version ZIPs.
- firebase-config.js is also omitted from 0.6.2; preserve the configured GitHub copy.

0.6 online baseline passed: iPad->iPhone, iPhone->iPad, and Read status.

0.6 offline bug: downloaded messages disappeared after offline kill/reopen and a queued cloud message was lost.

0.6.1 fixes retained:
- encrypted IndexedDB Outbox authoritative
- critical IndexedDB writes await transaction completion
- startup reconstructs missing queued messages from Outbox
- Firestore snapshots preserve local pending messages
- downloaded cloud messages persist immediately to encrypted local cache
- auth restoration retries Outbox
- Outbox removed only after confirmed Firestore write

0.6.1 first retest found a foreground listener bug:
- iPad sent successfully
- iPhone had FIDUNIO open but did not show the message
- killing/reopening iPhone made the message appear
This proved Firestore delivery succeeded but the active message listener had not attached/re-attached.

0.6.2 fix:
- attach active cloud message listener when Firebase Auth restores
- attach after Firebase initialization
- re-attach on reconnect
- re-attach on iOS/PWA visibility resume and pageshow
- retain all 0.6.1 reliability fixes
- cloud header remains Connected, not Secure, until E2EE

Test order: foreground live receive both directions first; only then offline cache, Outbox survival, reconnect delivery.

0.6.2 remains plaintext in Firestore and is not E2EE. Use harmless test messages only.
