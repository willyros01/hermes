# FIDUNIO Account-Authoritative Group E2EE v1

Status: normative rebuild contract.

## Ownership

Group message cryptography is owned by one future `e2ee-account-group-runtime.js` module. `firebase.js` remains the sole Firebase SDK/service owner and transports opaque encrypted records only. `app.js` may orchestrate UI state but MUST NOT derive group keys, encrypt/decrypt group payloads, or write Firestore directly. The service worker remains cache/transport only.

## Identity authority

Every group member uses the same durable account E2EE P-256 identity already defined by `ACCOUNT-E2EE-FIRESTORE-AUTHORITY.md`. Device IDs MUST NOT participate in group routing, key derivation, AAD, or decryptability.

A group has an integer `keyEpoch`. Membership changes MUST increment `keyEpoch` before new messages can be sent. An epoch key is a random 32-byte AES key generated by the serialized group-key owner; it is never stored in plaintext in Firestore.

## Epoch-key distribution

For each active member UID, including the sender, store one account-encrypted epoch-key envelope. The envelope is derived using account ECDH P-256 plus HKDF-SHA256 and AES-256-GCM. The recipient is identified by UID and durable account `keyId`, never by device ID.

Required epoch record fields:

- `format`: literal `fidunio-group-key-v1`
- `groupId`
- `keyEpoch`: positive integer
- `createdByUid`
- `createdByKeyId`
- `memberKeyIds`: map of UID -> durable account keyId
- `envelopes`: map of UID -> `{senderKeyId,recipientKeyId,ciphertext,iv}`

No plaintext group key is permitted in Firestore, localStorage, logs, URLs, notifications, or service-worker messages.

## Message format

Group messages use `e2ee: 4` so they cannot be confused with direct-message `e2ee: 3`.

Required Firestore message fields:

- `e2ee`: literal `4`
- `groupFormat`: literal `fidunio-group-message-v1`
- `keyEpoch`: positive integer
- `senderUid`
- `senderKeyId`
- `ciphertext`
- `iv`
- `state`
- `createdAt`
- optional display-only `timeLabel`

The encrypted plaintext payload is canonical UTF-8 JSON containing at minimum `{version:1,groupId,messageId,senderUid,text}`. Future attachment metadata may be added inside this encrypted payload without changing the outer transport contract.

AAD MUST bind exactly: `FIDUNIO-GROUP-V1|<groupId>|<messageId>|<keyEpoch>|<senderUid>|<senderKeyId>` encoded as UTF-8. Device identifiers are forbidden.

## History policy

The existing `historyPolicy: "fromJoin"` is mandatory by default. A newly added member receives an epoch-key envelope only for the new epoch created after membership activation and therefore cannot decrypt earlier epochs. Explicit administrator history sharing, if implemented later, MUST be a separate deliberate operation that wraps selected older epoch keys for that member and records the grant; it must never happen implicitly.

Removing or deactivating a member MUST rotate to a new epoch before another message is accepted. The removed member receives no envelope for the new epoch.

## Offline / Outbox

The Outbox is authoritative for pending sends. A queued group message records the target `groupId`, `messageId`, plaintext payload inside the existing installation-local encrypted Outbox, and the expected `keyEpoch`. On retry, the runtime MUST re-read group membership/epoch authority. If the epoch changed, it MUST encrypt under the current authorized epoch rather than send ciphertext for a stale membership set. The Outbox record is removed only after Firestore confirms the message write.

## Receipts

A single shared mutable `state` field is insufficient for group Delivered/Read semantics. Group receipts MUST be per-account records keyed by UID. A sender's UI computes aggregate state from the active recipients for that message epoch. The sender cannot write another member's receipt.

## Failure behavior

Fail closed when the durable account identity, current group membership, current epoch record, required member public keys, or recipient envelope is missing/inconsistent. Never fall back to plaintext, direct-message v3, legacy per-device E2EE, or a newly generated replacement identity.

## Required implementation order

1. Firestore rules/schema for group epoch keys, encrypted messages, and per-account receipts.
2. `firebase.js` opaque group transport APIs under the existing Firebase owner.
3. `e2ee-account-group-runtime.js` as sole group crypto owner.
4. Outbox integration with epoch revalidation.
5. `app.js` group send/receive/read UI integration.
6. Rule/emulator and runtime tests before enabling the currently disabled group Send path.

## Atomic group-administration rule — 2026-09-06

Group membership mutation and the replacement epoch are one serialized operation. Add/remove/leave prepares the epoch for the exact post-change member UID set, then `firebase.js` commits parent membership/admin arrays, `keyEpoch + 1`, member-subcollection mutation, and the matching epoch document atomically. There is no state in which changed membership can accept messages under the old epoch. A removed/leaving member is absent from the replacement epoch envelopes. The owner remains immutable and cannot be removed or leave until an explicit ownership-transfer design is approved.
