HERMES PROJECT — FIDUNIO PRODUCT
CUMULATIVE PROJECT MEMORY
Protected rollback baseline: FIDUNIO 0.8.1.16 at commit 2a6380e26ce969fc95f0a38e29517e48b09b781b
Current validated development baseline: FIDUNIO 0.9.0.4
Current live test build on main: FIDUNIO 0.9.2.3
Development branches retained: release/0.9.0 and release/0.9.1
Internal project/repo name: Hermes
Public app/product name: FIDUNIO — Private Messaging

LOCKED CONVENTIONS
- Hermes = internal project/repository/release ZIP naming; FIDUNIO = public app/product.
- Keep release files flat in repository root.
- Keep exactly one cumulative root hermes-memory.txt and one reusable root hermes-setup.txt; no version-numbered memory/setup TXT.
- firebase-config.js and config-firestore.js must NEVER be regenerated, overwritten, or included in update/release ZIPs.
- version.js is the single authoritative runtime version source; every test build gets a distinct visible version.
- Preserve winged-messenger artwork, navy/teal/gray palette, no gold.
- Preserve iPhone single-pane and adaptive iPad two-pane behavior; responsive by viewport width.

STABLE BASELINES
- 0.6.5 local-first/offline PASS; 0.7.3 direct E2EE transport/reconnect PASS; 0.8.0 device identity PASS; 0.8.1 contact key verification PASS.
- UI 0.8.1.9 plus Group Info tablet fix PASS.
- 0.8.1.16 receipt/re-entry PASS and protected rollback.
- 0.9.0.2 per-device E2EE two-device + receipt/re-entry + offline Outbox/reconnect PASS.
- 0.9.0.4 full three-device bidirectional per-device E2EE fan-out PASS and remains validated development baseline.
- Preserve 0.9.0.4 messaging, receipt, Outbox and per-device fan-out unchanged.

0.9.1 GROUP FOUNDATION
- Separate /groups schema/rules and real-user group metadata UI are in place. Group message writes remain disabled until group E2EE/key rotation is implemented.
- 0.9.1.1 real New Group user list PASS. 0.9.1.2 prototype/mock conversation cleanup PASS.
- bootstrap.js removes only known prototype local data while preserving real cloud conversations, E2EE keys, device identities, settings, peer trust and real history.

0.9.2 INVITE / PROFILE / SETTINGS
- Invite-only account admission implemented. Alpha claimed permanent system Owner. Owner/Admin can create single-use expiring User/Admin invitations.
- Forgot Password and authenticated Change Password implemented.
- Profile fields displayName, email, telephone and photoURL save to /users/{uid}; email/password changes require current-password reauthentication.
- Firebase Account and Device Identity technical cards are collapsed by default with Copy Details.
- 0.9.2.2 attempted display-name propagation by rewriting conversation memberNames from the renamed user's client, but live test FAILED because Firestore conversation update rules only allow a conversation member and the approach did not reliably refresh peer UI. Do not rely on memberNames as authoritative profile name.
- 0.9.2.3 changes peer display-name propagation to READ the authoritative /users/{peerUid}.displayName on each device via live profile listeners and overlays the peer name in conversation/chat UI. This avoids requiring cross-document conversation metadata writes and leaves E2EE/message transport untouched.
- 0.9.2.3 adds a visible Sign Out button directly on the main Messages/chat screen, not hidden inside Firebase Account.
- 0.9.2.3 forces About to be the absolute final Settings child, after the version footer and all dynamically-added Profile/Invitation cards.
- firebase-config.js was not touched.

NEXT LIVE TEST
1. Refresh/reopen and confirm visible FIDUNIO v0.9.2.3.
2. On iPhone/Beta, confirm Alpha's already-saved new display name replaces Alpha Account without changing the name again.
3. Confirm Sign Out is visible on the first Messages/chat screen.
4. Open Settings and confirm About is physically the bottom-most section.
5. If these pass, continue admin/security lifecycle work before Gamma onboarding: invitation revoke, user suspend/restore/expiration, security-event audit, then inactivity PIN/passkey lock.

SECURITY / ADMIN PLAN
- Account lifecycle: Active → Suspended → Deactivated; avoid destructive hard-delete by default.
- Add Admin Users page, invitation revoke, expiration, suspend/restore and audit/security events.
- Firestore access should eventually require active/unexpired status. True Firebase Auth disable/delete requires privileged backend/Admin SDK rather than client-only GitHub Pages.
- Local inactivity lock design: Firebase session may remain authenticated while FIDUNIO UI locks; unlock with local PIN verifier or standards-based WebAuthn/passkey/biometrics; configurable idle timeout and Lock Now.

IMPORTANT TECHNICAL DETAILS
- app.js imports firebase.js; firebase.js imports protected firebase-config.js.
- index.html loads version.js then bootstrap.js. bootstrap loads settings-polish.js, auth-ui.js, profile-sync.js and main-screen-polish.js.
- IndexedDB fidunio-local v2; encrypted local state/Outbox/history. Firebase data is never service-worker cached.
- E2EE v2 uses per-device P-256 ECDH + HKDF SHA-256 + AES-GCM envelopes; e2ee:1 legacy history compatibility retained.
- Account-level compatibility trust is not final multi-device trust design; future device-level approval/safety numbers remain required.
