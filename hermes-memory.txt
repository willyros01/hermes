HERMES PROJECT — FIDUNIO PRODUCT
CUMULATIVE PROJECT MEMORY

MANDATORY FIRST READ — BEFORE ANY CODE CHANGE
- Every future chat/coding session MUST begin by reading supporting repo documentation. Do not rely on conversational memory alone.
- At minimum: `hermes-memory.txt`, `CODING-GUIDELINES.md`, `ACCOUNT-E2EE-FIRESTORE-AUTHORITY.md`, `E2EE-V1-CRYPTO-FORMAT.md`, `FIRESTORE-E2EE-V1-RULES.md`, `FIRESTORE-E2EE-V1-EMULATOR-TESTS.md`, `E2EE-RECOVERY-PROTOCOL.md`, `FIREBASE-RECOVERY-PROJECT-CONFIG.md`, `DETERMINISTIC-UI-LIFECYCLE.md`, `RUNTIME-CONSOLIDATION-PLAN.md`, `RUNTIME-TRANSFORM-INVENTORY.md`, `RUNTIME-AUTHORITY-MAP.md`, `REBUILD-BASELINE-AUDIT.md`, `architecture-ownership.txt`, `BUG-LIST.md`; E2EE work also reads `E2EE-IDENTITY-LIFECYCLE.md`.
- Core: ONE RESOURCE -> ONE OWNER -> ONE PREDEFINED AREA -> ONE SERIALIZED WRITE PATH.

REBUILD BASELINE — SEPTEMBER 5, 2026
- User authorized moving from security proving into repository cleanup/rebuild and asked to keep working until actual input is needed.
- Protected working branch: `fidunio-rebuild-baseline-2026-09-05`, created from `main` commit `246c524abc78404d9bc744b272ce08244a35cc3f`. `main` remains rollback/reference while rebuild proceeds.
- `REBUILD-BASELINE-AUDIT.md` is the authoritative keep/remove/rebuild map; `RUNTIME-AUTHORITY-MAP.md` records current resource owners and temporary ownership violations.
- Protected checkpoints:
  - `checkpoint-rebuild-baseline-recovery-pass` -> `e2d2e10031182781f6887b2cd1a971701aa21e3a`
  - `checkpoint-rebuild-new-message-owner` -> `b4731926432af3a986af991d2f51db86acea0fd1`
- Removed confirmed dead/superseded one-off artifacts from rebuild branch only: baseline cleanup/reset pages/scripts, E2EE diagnostics, one-shot cleanup workflow, superseded auth/admin/invite/settings-polish modules, historical `test-0.9.0/`, and observer-based `new-message-polish.js` after its behavior was materialized.
- Rebuild target is forward consolidation, not historical rollback.

FIRESTORE E2EE V1 GATE — EXACT SCHEMA
- Firebase Local Emulator Suite gate uses demo project `demo-fidunio-e2ee-rules`, never production.
- Current matrix contains 42 assertions: original 40 private/public/atomic/regression cases plus rejection of generic recovery `metadata` and public JWK `ext`/`key_ops`.
- Exact public JWK is `{kty,crv,x,y}` only.
- Exact recovery wrapper fields are: `version,ciphertext,iv,wrappedRecoveryKey,wrappingAlgorithm,recoveryAuthorityVersion,recoveryKeyIv,recoveryKeyWrappingAlgorithm`; generic metadata forbidden.
- Main rules run `33974521992` at commit `246c524abc78404d9bc744b272ce08244a35cc3f` succeeded.
- Rebuild branch security gate re-runs the same emulator test. Repository validation does NOT mean live Firebase rules are deployed.

ACCOUNT E2EE IDENTITY MANAGER — VALIDATED
- `e2ee-account-identity-manager.js` is sole proposed account identity lifecycle owner/state machine and remains isolated from normal production UI/runtime.
- User-reported iPad/Safari browser gate PASSED: ALL 24 TESTS PASSED.
- Manager includes sign-out generation invalidation so in-flight work cannot repopulate runtime after reset.
- Ordinary runtime private key is non-extractable.
- `e2ee-account-firestore-adapter.js` receives already initialized central Firebase services and never initializes Firebase independently.
- `e2ee-account-firebase-adapter.js` central exposure has a passing CI gate.

E2EE V1 CRYPTO
- iPad Safari crypto validation passed all isolated tests.
- Frozen: ECDH P-256; stable random keyId; transient PKCS#8 only; AES-256-GCM wrappers; PBKDF2-HMAC-SHA256 600000; random 16-byte salt; random 12-byte IV; exact six-digit PIN; canonical JSON KDF/AAD; base64url without padding.

RECOVERY SERVER — VALIDATED REPOSITORY IMPLEMENTATION
- Recovery uses random 256-bit RUK; client recovery wrapper + PIN/server-master protected RUK. No plaintext password/PIN/RUK/private key/master secret in Firestore.
- Exact v1 server KDF bytes: UTF8(JSON.stringify(["FIDUNIO-E2EE-RECOVERY-KDF",1,uid,keyId,pin])). HMAC-SHA256 keyed by the 32-byte recovery master secret produces the AES-256-GCM RUK wrapping key.
- Exact server RUK AAD bytes: UTF8(JSON.stringify(["FIDUNIO-E2EE-RECOVERY-RUK",1,uid,keyId])).
- Server RUK wrapping uses AES-256-GCM, random 12-byte IV, 16-byte tag, base64url without padding.
- Recovery session: 10 minutes; max 5 PIN failures/session; max 10 consecutive account PIN failures; new session does not reset account counter; success resets counter.
- Recovery crypto/session/callable/persistence tests pass in `Rebuild Baseline Security Gate`.

CLOUD FUNCTIONS RECOVERY SCAFFOLD — PREPARED, NOT DEPLOYED
- Authoritative server source lives under `functions/recovery/`. Root `e2ee-recovery-*.mjs` files are compatibility re-exports so existing tests exercise deployable source rather than a duplicate implementation.
- `functions/package.json`: Node.js 22, `firebase-functions` 7.3.2, `firebase-admin` 14.3.0.
- `firebase.json` registers Functions codebase `recovery` with source `functions`.
- `functions/index.mjs` exports v2 callables `enrollRecoveryV1`, `startE2EERecoveryV1`, `completeE2EERecoveryV1`.
- App Check enforcement is enabled; completion is marked for limited-use App Check token consumption.
- Secret `FIDUNIO_RECOVERY_MASTER_V1` is declared with `defineSecret` and bound only to enrollment/completion.
- Master secret format expected by scaffold: exactly 32 random bytes represented base64url without padding.
- `completeE2EERecoveryV1` is intentionally FAIL-CLOSED until supplemental recovery verification is implemented/reviewed; accidental scaffold deployment cannot release a RUK.
- No Cloud Function, Secret Manager secret, IAM change, App Check setting, billing change, or Firebase production deploy has occurred.

FIREBASE PROJECT CONFIGURATION GATE
- `FIREBASE-RECOVERY-PROJECT-CONFIG.md` records the exact future live-project handoff.
- Project-owner input is not needed until actual project confirmation/billing/region/Secret Manager/IAM/App Check/live deployment is required.
- Current scaffold region is `us-central1`; confirm final region against actual Firebase/Firestore project before first deployment.
- Supplemental recovery verifier remains the only intentional fail-closed server-code gap before enabling RUK release.
- User plans to use Gemini as a Google/Firebase console operator while ChatGPT remains in the loop and supplies exact prompts/commands; no Gemini-side project changes should occur until repo-side handoff is ready.

DETERMINISTIC UI / CURRENT RUNTIME
- `bootstrap.js` is now startup sequencing only and contains no broad MutationObserver scrub path.
- Raw `app.js` now starts from `contacts=[]`, supports a genuine empty Messages state, and calls explicit `new-message-owner.js` for recipient selection.
- Superseded observer-based `new-message-polish.js` has been deleted.
- `main-screen-polish.js` remains a TEMPORARY MutationObserver compatibility layer for Sign Out + peer-name projection, but its Sign Out now routes through central `signOutFidunio()` instead of independently importing Firebase Auth.
- `profile-sync.js` remains a TEMPORARY independent Firebase SDK reader for live peer profile names; consolidate through `firebase.js` before removal.
- `settings-lifecycle-bridge.js` remains temporary until `app.js` exposes final Settings render ownership.
- `runtime-authority-gate.test.mjs` now prevents new direct Firebase SDK ownership or new MutationObserver repair paths outside the explicit temporary allowlist and checks that New Message/empty-state materialization does not regress.

SERVICE WORKER / HIDDEN DUAL CODE
- `service-worker.js` still rewrites `app.js` at runtime.
- Remaining transforms are now explicitly classified in `RUNTIME-TRANSFORM-INVENTORY.md`.
- Non-cryptographic group imports/state/subscriptions/UI/send guard are the next safe materialization target.
- Per-device E2EE v2 helper/receive/trust/send transforms are legacy compatibility and MUST NOT simply become the target architecture. They remain until account-authoritative send/receive replaces them with deliberate legacy migration support.
- Final service worker must perform shell/network/offline caching only and never alter JavaScript semantics.

OFFLINE MESSAGING TARGET
- Firestore = durable encrypted authority; UID-scoped local cache = rebuildable offline copy; Outbox = temporary pending-send authority.
- Reconnect is serialized/idempotent with stable message IDs.

CURRENT REBUILD ORDER
1. COMPLETE — crypto/identity-manager/Firestore/recovery security proving.
2. COMPLETE — initial repository dead-code cleanup and recovery Cloud Functions scaffold/documentation.
3. COMPLETE — deterministic empty Messages state + explicit New Message owner + rollback checkpoint; observer-based New Message polish removed.
4. CURRENT — materialize group metadata/subscription/UI/send guard from service-worker transform into authoritative source as one bounded non-cryptographic unit.
5. Replace peer-name/main-screen observer projections with central Firebase APIs + explicit app projection; remove remaining temporary observer/SDK ownership.
6. Wire validated account E2EE through central `firebase.js` ownership.
7. Replace legacy per-device E2EE send/decrypt/fan-out transforms boundedly with account-authoritative messaging and explicit migration support; device ID remains informational target data only.
8. Reduce service worker to cache/offline only.
9. Build/finish Firestore-authoritative encrypted history + UID-scoped rebuildable cache + Outbox.
10. Revalidate iPhone/iPad/PWA receipts/offline/Settings/account switching/reinstall/recovery.
11. Live Firebase Functions/Secret Manager/App Check/IAM/rules deployment stops for user input only when actual project action is required.

PIN / LOCAL SECURITY
- Existing local installation PIN remains 4–12 digits with its current local-security design. New account E2EE PIN is exactly six digits. Reconcile deliberately later.
- Remove Local PIN bug remains open.

PROTECTED BASELINES / CONVENTIONS
- Preserve 0.9.4.11 stable baseline, 0.9.5.1 Settings, 0.9.5.4 receipts, 0.9.5.7 identity race fix.
- Rejected 0.9.4.12-.15 and rejected invite-install work remain forbidden.
- Hermes internal/FIDUNIO public; cumulative `hermes-memory.txt`; never overwrite/regenerate `firebase-config.js` or `config-firestore.js`; `version.js` authoritative; preserve artwork/navy-teal-gray/no gold/accessibility/display-name UX.

FINAL HANDOFF
Read supporting repo docs before code. Repository docs are durable authority. DETERMINISTIC: reduce competing authorities; do not add repair machinery.
