FCM N3 REPOSITORY CANDIDATE — 1.1.1: Deterministic Settings now has a Notifications panel. Permission is requested only from the explicit Enable Notifications user gesture. One installation ID is retained locally; firebase.js remains sole Firebase owner and reads/writes only users/{uid}/notificationDevices/{installationId}. Exact owner-only Firestore rules + emulator matrix are included. Public VAPID configuration remains intentionally empty in notification-config.js pending the controlled Google/Firebase handoff; no live rules or Messaging configuration has been changed. N2 stale attachment-cache regression assertion was made release-agnostic without weakening attachment purge coverage. Disappearing attachments are device accepted across direct photo/file/audio/video and group photo, including reopen anti-resurrection.

FCM N2 OWNERSHIP FOUNDATION (1.1.0): Start Item 3 under authoritative `FCM-NOTIFICATION-ARCHITECTURE.md`. `firebase.js` remains sole Firebase initializer and owns bounded Messaging SDK capability/get-token/delete-token APIs. `notification-registration.js` is a dependency-injected registration owner with no Firebase imports and no permission prompt; `notification-policy.js` restricts status and opaque route metadata. N2 makes no live Firebase/Google change and does not activate notifications. Item 2 disappearing attachments is DEVICE ACCEPTED/CLOSED after direct photo, file, audio, video and group photo 5-minute tests all disappeared correctly after authoritative Read and did not return after close/reopen.

DISAPPEARING ATTACHMENTS CANDIDATE (0.9.9.19): Extend the existing disappearing selection to future photos/files/audio/video using the same immutable duration + purge version. The existing serialized server purge executor remains sole coordinator; its one repository now also receives the existing Admin Storage bucket and deletes deterministic ciphertext prefix `attachments/{senderUid}/{conversationId}/{messageId}/` before final source deletion, with authoritative basis revalidation before Storage mutation and again before Firestore source deletion. Local UID-scoped purge releases attachment object URLs/runtime entries. No browser Storage delete, tombstone, second scheduler, or second Firebase owner. Live backend handoff `att.txt` is required before device testing. EVERY release bumps visible version + service-worker shell revision.

DATE/TIME DEVICE ACCEPTANCE (0.9.9.18): User confirmed group date, group sender name/time, and direct one-to-one date separator are displaying correctly. Close those presentation items.

DIRECT DATE TIMESTAMP CORRECTION (0.9.9.18): 0.9.9.17 failed device acceptance because direct-message projection omitted Firestore `createdAt`; renderer therefore had no day key. Preserve `createdAt` in the existing direct projection. Date rendering remains one shared owner for direct and group. Visible version and service-worker shell revision must both bump on every release.

DIRECT DATE SEPARATORS (0.9.9.17): Apply the same horizontal-line `Month D, YYYY` day demarcation used in groups to one-to-one/direct conversations. Message times remain unchanged. Group sender name/time remains accepted. No messaging authority changes. Every release must bump both visible version and service-worker shell revision so installed PWAs flush/retire the prior shell cache.

GROUP SENDER TIME CORRECTION (0.9.9.16): Device evidence showed 0.9.9.15 sender names but no sender-line time on existing group messages. Root cause: some projected group rows have empty legacy `time` while authoritative `createdAt` remains available. The display now resolves explicit `time` first and falls back to `createdAt` formatted as h:mm AM/PM. Date separators and messaging authority are unchanged.

GROUP SENDER TIME CANDIDATE (0.9.9.15): Preserve 0.9.9.14 group date separators. For each group message, render the message's existing display time immediately beside the resolved sender name (for example `Willy  10:42 PM`). Keep the existing per-message time metadata as well; this request is additive. Direct-message sender presentation remains unchanged. No messaging/Firebase/E2EE authority changed.

GROUP DATE SEPARATOR CANDIDATE (0.9.9.14): Group chats render a horizontal-line date demarcation (`Month D, YYYY`) at the first visible dated message and each calendar-day change. Every individual message retains its existing time-only metadata. Sender-name labels remain unchanged. Date grouping uses projected `createdAt`; optimistic outgoing rows receive a local `createdAt` immediately. No send/E2EE/Firebase authority changed.

GROUP SENDER LABEL CANDIDATE (0.9.9.13): Group bubbles now show the sender's FIDUNIO display name by resolving the existing group-message `senderUid` against the already-owned group member projection in `app.js`. This is presentation-only; no second Firebase/profile subscription or message authority was added. Incoming and outgoing group rows receive a label; direct-message bubbles remain unchanged. Permanent gate `group-sender-label.test.mjs` protects senderUid projection, group-only rendering, and removal of the old incoming-only/m.sender dependency. Device acceptance on iPhone/iPad is still required before closing this outstanding item.

OUTSTANDING FOLLOW-UP ITEMS (2026-09-08): (1) Disappearing attachments for photos/files/audio/video are still OPEN. They must not be activated until server-side Firebase Storage manifest/chunk deletion and all related trace cleanup satisfy the binding `DISAPPEARING-PURGE-AUTHORITY.md` source-delete-last contract, with permanent tests, live deployment verification, and device acceptance. (2) Group chat sender labeling is OPEN: each group-message bubble must show the sender's FIDUNIO display name; direct-message bubble layout must remain unchanged. These items must remain visible in the durable checklist until separately built and accepted.

DISAPPEARING TEXT FEATURE DEVICE CLOSEOUT (2026-09-08): FIDUNIO 0.9.9.12 passed the group real-device acceptance test after the already accepted direct purge and unread-protection tests. Group message sent; all tested recipients received it; all required Reads were confirmed; the message disappeared from sender and all recipients; after close/reopen it did not return. Disappearing TEXT is now device accepted and closed for this activation checkpoint. Attachments remain intentionally NON-disappearing until server Storage trace deletion satisfies the source-delete-last contract and receives separate activation/device acceptance. Preserve `DISAPPEARING-PURGE-AUTHORITY.md` as binding authority for any future disappearing-content work.

DISAPPEARING UNREAD-PROTECTION DEVICE ACCEPTANCE (2026-09-08): FIDUNIO 0.9.9.12 passed the unread-protection real-device test. A 5-minute disappearing text remained present beyond five minutes while the recipient had not Read it, proving expiry does not start from send or delivery time. Direct disappearing-text purge and unread protection are accepted. Do not mark the overall disappearing-text feature closed until group acceptance passes. Attachments remain intentionally non-disappearing in this activation checkpoint.

DISAPPEARING DIRECT DEVICE ACCEPTANCE (2026-09-08): FIDUNIO 0.9.9.12 passed the fresh direct 5-minute real-device test after scheduler/index recovery. Message sent; recipient produced authoritative Read; message physically disappeared on both sender and recipient; after both apps were closed/reopened the message did not return. Direct text disappearance is accepted. Do not mark the entire disappearing feature closed until unread-protection and group acceptance also pass. Attachments remain non-disappearing in this activation checkpoint.

DISAPPEARING LIVE INDEX RECOVERY (2026-09-08): Real-device Test 1 exposed no purge after Read. Live Function logs proved the scheduler query failed with FAILED_PRECONDITION because `messages.disappearingPurgeVersion` lacked a COLLECTION_GROUP ASCENDING index. `firestore.indexes.json` is durable authority for that index and `firebase.json` must reference it. The reviewed index was deployed to `fidunio-fef13`; after activation, scheduler `firebase-schedule-purgeDisappearingMessagesV1-us-central1` was ENABLED with empty status.code. Future disappearing scheduler deployment must verify the live query/index boundary, not merely Function ACTIVE + scheduler existence. Fresh device acceptance remains required.

### DISAPPEARING LIVE INDEX REPAIR — 0.9.9.12

Real-device Test 1 proved send and authoritative Read but no purge. Live Function logs isolated the backend failure to `FAILED_PRECONDITION`: the scheduler collection-group query on `messages.disappearingPurgeVersion` lacked its required `COLLECTION_GROUP ASCENDING` Firestore field index. `firestore.indexes.json` is now durable deployment authority for this index and `firebase.json` explicitly references it. Future disappearing scheduler deployment must include Firestore indexes and must verify a successful post-index scheduler invocation; ACTIVE Function + existing scheduler alone is insufficient live proof.

HERMES PROJECT — FIDUNIO PRODUCT
CUMULATIVE PROJECT MEMORY

### DISAPPEARING TEXT ACTIVATION — 0.9.9.12

Disappearing text is activated only through the binding `DISAPPEARING-PURGE-AUTHORITY.md` ownership path. New text rows receive `disappearingPurgeVersion: 1`; pre-activation rows and attachments do not. `purgeDisappearingMessagesV1` is the only scheduled server discovery owner and calls the existing purge executor/revalidated server repository. Browser clocks and client delete rules remain prohibited. Before any future disappearing-content change, reread `DISAPPEARING-PURGE-AUTHORITY.md` plus the normal mandatory architecture documents. Attachment disappearance remains intentionally unactivated until server Storage deletion can satisfy the trace-free source-delete-last contract. LIVE BACKEND ACTIVATION COMPLETE: reviewed Firestore rules, dedicated purge IAM/runtime identity, and scheduled Function `purgeDisappearingMessagesV1` were deployed and verified through the controlled Cloud Shell handoff. Function state is ACTIVE in `us-central1`; runtime SA is `fidunio-disappearing-purge@fidunio-fef13.iam.gserviceaccount.com`; scheduler `firebase-schedule-purgeDisappearingMessagesV1-us-central1` is present. Do not mark disappearing text fully accepted until the direct/group/unread/offline device matrix passes.


### MESSAGE NOTIFICATION / FCM AUTHORITY — FIDUNIO 1.1

`FCM-NOTIFICATION-ARCHITECTURE.md` is the authoritative durable design for every FIDUNIO Message Notification / FCM / Web Push task. BEFORE any notification implementation, testing, deployment, debugging, Firebase Messaging configuration, token-registration change, Cloud Function notification work, service-worker notification handling, or notification-click routing, reread that document together with the normal mandatory architecture documents. Do not rely on conversational memory. If a future notification proposal conflicts with that document, stop and reconcile the architecture before coding.

Governing notification principle: FCM tells the user that FIDUNIO has something new; Firestore plus the existing account-authoritative E2EE messaging system remain the only authority for what the message actually is. FCM carries no plaintext message/attachment content and is never a second message, receipt, E2EE, Firebase-init, or persistence authority. FCM activation remains FIDUNIO 1.1 work. No live Firebase/Google configuration change was made when this architecture was approved/documented.


### IOS LONGER CAMERA CAPTURE CONTINUITY — 0.9.9.9x

Issue 1 remained open after 0.9.9.9w: 10-second/~1.5-MB camera videos sent and received on iPhone and iPad, but 20-second captures on both devices returned with no bubble, warning or trace. Because preflight validation itself never appeared, the failure boundary was the native-picker callback. Build 0.9.9.9x mounts the temporary file input in the document and marks the system picker active. The local security monitor does not lock/re-render while that picker owns the camera; it resumes normal behavior immediately when change or cancel closes the picker. This narrow exception must not weaken normal leave-app locking. Issues 2 (LTE optimistic visibility) and 3 (iPhone composer overlap/scroll anchoring) remain deferred and untouched.

### IPHONE CAMERA-VIDEO FAILURE PATH — 0.9.9.9w

The user directed that three reported issues be handled separately and in order: (1) camera-video send failure, (2) sent LTE message absent from the conversation until delayed reconciliation, and (3) the last iPhone lines covered by the widgets/composer with non-sticky scrolling. Build 0.9.9.9w addresses only issue 1. Attachment validation now precedes creation of the temporary preview and local message. An invalid/over-limit camera video (current video limit 50 MiB) shows the true selection error and leaves no partial row. Failures occurring after valid staging render as send failures and never invoke the attachment receiver. Issues 2 and 3 remain untouched for later, in that order.

### ATTACHMENT UPLOAD PUBLICATION BARRIER — 0.9.9.9v

A real iPhone camera test exposed `Chunk 8 download failed (storage/object-not-found)` after restart even though the immediate sender preview had displayed. The encryption/chunk plan was sound; the app's `stageEncryptedOutbox` callback incorrectly inserted the attachment message into the normal publishable Outbox before Storage upload completion. The fixed order is local encrypted staging, upload manifest and every chunk, verify metadata for every object, then queue/publish the direct or group message. Existing messages missing a chunk cannot be reconstructed and must be deleted/resubmitted. Original-photo transport and the library/camera chooser remain enabled.

### ORIGINAL PHOTO + SOURCE CHOICE — 0.9.9.9u

By user direction, photo compression is disabled after encrypted-picture download performance became acceptable. A selected image now retains its original bytes, format, dimensions, and size before encryption. The Photo attachment tool must first present large choices for Photo Library or Take a Picture; library selection omits the capture attribute and camera selection requests the environment camera. The existing immediate sender preview, E2EE, Storage upload, CORS repair, download, and recipient decryption paths remain authoritative.

### ATTACHMENT STORAGE CORS DIAGNOSIS — 2026-09-08

Attachment acceptance on visible build 0.9.9.9t changed the error from the old SDK timeout to `Manifest download failed (storage/unknown): Load failed` on the recipient and on the sender after its temporary local preview was lost. This proves the durable Storage object path is being used and isolates the failure to Safari's cross-origin Storage fetch. Root script `s.txt` applies a narrow bucket CORS entry for `https://willyros01.github.io`; it does not weaken Storage rules, Firestore rules, authentication, membership authorization, or E2EE. Acceptance remains open until a new photo displays on both devices and still displays after sender restart.

### BASIC DIRECT-MESSAGE PRIORITY — 2026-09-07

The user explicitly ended further audit/design/key-management cycling. Immediate goal: prove ordinary one-to-one message transmission. Minimal candidate removes account-key preparation and fingerprint verification as prerequisites for new direct text, using the authenticated member-only plaintext format already accepted by `firestore.rules`. Existing encrypted rows remain readable. Do not expand this slice into App Check, groups, attachments, deletion, or further security redesign. Required exit is full gate/Pages followed by real two-device Sent → Delivered → Read in both directions.

The first iPad check still showed a legacy encryption-key-changed fingerprint banner. Ordinary direct chat must show no key status, verification prompt, or Conversation Security screen. Its Info button opens only plain Chat Info.

First basic transport evidence: iPad message reached and was read on iPhone, but iPad stayed Sent. This passes transmission and isolates return receipt. Direct-chat opening now invokes the existing recipient-only conversation Read owner explicitly so cached initial snapshots cannot suppress the Firestore Read update.

That first receipt repair failed after two iPhone restarts. The missing event was cache-to-server metadata confirmation: default Firestore listeners may display cached content without firing again solely because `fromCache` changes. Candidate 0.9.9.8c enables metadata-change callbacks and preserves real metadata on listener reuse. From now on every device candidate must advance a visible version suffix so the user can verify what is installed.

0.9.9.8c device proof still left all iPad rows Sent. The iPad listener was awake—the conversation timestamp advanced to 8:10 PM—so receiver Read was not written. Plaintext snapshot processing still awaited obsolete compatibility-key lookup. Candidate 0.9.9.8d skips that lookup unless a legacy encrypted row exists, reads explicit receipt recovery from Firestore server authority, and propagates failures instead of `Promise.allSettled` concealment.

0.9.9.8d failed four real-device receipt attempts. Treat it as rejected. Candidate 0.9.9.8e removes the `!meta.fromCache` gate: if an unread incoming row is displayed in the current direct chat, immediately invoke the existing receipt write. Any failure must appear as `Read receipt failed: ...`; never swallow it.

0.9.9.8e revealed the exact iPhone error: `Missing or insufficient permissions.` Therefore the receipt callback is running and Firebase rules are the blocker. Prior account-message rules tests covered plaintext create only, not plaintext Read update. Add that exact emulator case, then align/deploy the reviewed Firestore rule rather than changing listener logic again.

LIVE FIRESTORE RULES ALIGNMENT COMPLETE: Cloud Shell deployed and verified repository rules to `fidunio-fef13`. New ruleset `6f6d9fda-0698-48db-b1a9-b9dde31b9456` replaced `52ea515e-359f-453f-8822-3c0f6ef2659a`. This was backend-only, so the device candidate remains 0.9.9.8e. Do not declare the receipt bug closed until iPad → iPhone Sent → Read is proven on devices.

READ RECEIPT DEVICE PASS: After the live rules alignment, the iPad sender changed to Read when the iPhone displayed the message. Preserve the 0.9.9.8e receipt listener and deployed rules path.

CURRENT CANDIDATE 0.9.9.9a: direct text again uses the validated account E2EE v3 envelope at the sole serialized Outbox send point; ordinary chat still exposes no key/fingerprint workflow. The existing pending Delete Message and sender-only Delete for Everyone UI are enabled. `pin-input.js` is the sole six-slot PIN widget used by signed-out login and local unlock; login uses email/password/PIN to unlock account encryption before app start. The delete callable remains a live-backend prerequisite; do not claim Delete for Everyone device success until `deleteDirectMessageForEveryoneV1` is deployed and proven.

0.9.9.9b corrects the 0.9.9.9a unlock regression. Ordinary remembered-session access is PIN or biometric only. Initial sign-in/setup/recovery may use password plus the one PIN. The READY non-extractable account E2EE CryptoKey is retained in UID/keyId/revision-bound local storage, restored only after local authorization, cleared on sign-out, and never overwritten by a second startup bind. A stale password wrapper may use one existing recovery-authority attempt to preserve and rewrap the same identity.

0.9.9.9c adds Delete for Me for accepted sent and received direct/group messages. It physically removes local message/history traces and retains a per-conversation hidden-ID decision inside encrypted local app state so authoritative cloud snapshots do not re-add the row on that device. It never calls cloud delete. Pending cancellation and sender-only server Delete for Everyone remain separate actions.

0.9.9.9d makes leaving the foreground an immediate local-lock event through both visibilitychange and iOS pagehide. The inactivity timeout still governs foreground inactivity. The tablet Groups tab now opens a dedicated Groups route listing existing groups and an explicit New Group action; it must never silently remain on a direct chat when no group exists.

0.9.9.9e makes the displayed snapshot callback the sole direct read-receipt owner. It removes the competing per-row write plus unconditional bulk write that could yield a false permission warning after another write already advanced Sent to Read. Auto appearance listens through modern addEventListener and legacy iOS addListener. Accepted-message delete choices are peers on one row; Cancel is separate below.

0.9.9.9f preserves an already-mounted local PIN unlock control across incidental render calls from live cloud callbacks. Partial PIN digits and focus must remain intact until the sixth digit or an explicit failed unlock remount; callbacks while locally locked must never reset entry to box one.

LOGIN-JOIN is implemented in 0.9.9.9g. Sign In displays only email and password and, after successful Firebase authentication, restores the saved UID/key-bound device identity and enters without PIN. Join is a separate screen with invitation code, display name, email, password, and a six-digit PIN; it creates the account, account E2EE identity, saved device identity, and local PIN before entry. Explicit sign-out preserves the encrypted UID-bound saved identity. PIN/biometric is reserved for later local unlock while the Firebase session remains signed in. A reset/new device without its saved identity is an explicit recovery exception and must not put PIN boxes back on ordinary Sign In.

0.9.9.9h implements the user-approved login-join selector prototype. Sign In and Join FIDUNIO are large side-by-side graphic tab tiles with line icons, and the active mode has an accent surface. They must remain visually distinct from the full-width form submission action below.

0.9.9.9i gives asynchronous action buttons consistent progress feedback. Bootstrap applies an accessible `is-busy`/`aria-busy` spinner whenever an enabled clicked action disables itself, retaining it until re-enabled or replaced. Send and password reset explicitly disable for their awaited work. Synchronous navigation does not simulate delay.

0.9.9.9j corrects group send feedback and duplicate submission. Send is single-flight and clears the composer before asynchronous encrypted Outbox work; group completion renders immediately. A stale Sending row with no remaining Outbox record falls back to Delete for Me. Group receipts no longer use a circular parent/receipt rule dependency; `g.txt` must deploy the reviewed rules before device acceptance.

0.9.9.9k enables Delete for Everyone on accepted group messages only for the original sender. The client gates on `message.mine`; the authenticated callable independently verifies the authoritative group message `senderUid` and current membership. It deletes receipts, attachments, history-grant copies (repairing grant metadata), and the source message. Recipients retain Delete for Me only.

AUTHORITATIVE BUILD WORKFLOW: every completed, validated FIDUNIO build is committed and pushed directly to `main` as an inseparable part of the build. Do not ask the user for a separate push confirmation and do not require manual application-file upload/download. Backend actions that require the user's authenticated Google Cloud Shell remain explicitly identified and use a short `.txt` deployment script.

The original 0.9.9.9k `d.txt` failed three times at its GitHub archive download with HTTP 404, before any Google deployment request. It is retired and replaced by `n.txt` revision 0.9.9.9k-c. The replacement avoids GitHub archives: it creates the Function source tree and downloads only the pinned reviewed files from `raw.githubusercontent.com`, prints its script version and source commit, retries transient downloads, then deploys and verifies the existing callable.

0.9.9.9l fixes the original group creator's `Unsupported group Outbox payload` failure. `ensureEpoch()` returns `{authority, epochKey}`, but Outbox preparation incorrectly read `epoch.keyEpoch`; the undefined value became null during JSON serialization and failed the bounded payload validator. It now records `epoch.authority.keyEpoch`. The administrator's failed attempt already initialized the group epoch, so no additional key-management action is required.

0.9.9.9m addresses the repeated error after 0.9.9.9l: malformed encrypted Outbox records created by the older build remain on the device and are retried. The bridge now accepts bounded group identity even when the legacy epoch is null; flush revalidates current server membership/epoch and re-encrypts the message. Authorization remains server-controlled.

MANDATORY FIRST READ — BEFORE ANY CODE CHANGE
- Every future chat/coding session MUST begin by reading supporting repo documentation. Do not rely on conversational memory alone.
- `FIDUNIO-BUILD-CHECKLIST.md` is the authoritative operational completion ledger and is a MANDATORY read before every build. Select work from NOT DONE/IN PROGRESS items and update its state/evidence after every substantive build run. Never mark DONE merely because code exists; DONE requires real-app integration plus applicable repository validation.
- At minimum: `hermes-memory.txt`, `CODING-GUIDELINES.md`, `ACCOUNT-E2EE-FIRESTORE-AUTHORITY.md`, `E2EE-V1-CRYPTO-FORMAT.md`, `FIRESTORE-E2EE-V1-RULES.md`, `FIRESTORE-E2EE-V1-EMULATOR-TESTS.md`, `E2EE-RECOVERY-PROTOCOL.md`, `FIREBASE-RECOVERY-PROJECT-CONFIG.md`, `DETERMINISTIC-UI-LIFECYCLE.md`, `RUNTIME-CONSOLIDATION-PLAN.md`, `RUNTIME-TRANSFORM-INVENTORY.md`, `RUNTIME-AUTHORITY-MAP.md`, `REBUILD-BASELINE-AUDIT.md`, `architecture-ownership.txt`, `BUG-LIST.md`, `DEVICE-ACCEPTANCE-BUGS.md`; E2EE work also reads `E2EE-IDENTITY-LIFECYCLE.md` and `ACCOUNT-E2EE-DIRECT-MESSAGE-FORMAT.md`.
- Message Notification / FCM / Web Push work additionally MUST read `FCM-NOTIFICATION-ARCHITECTURE.md` before any consequential design, code, test, deploy, or Google/Firebase configuration instruction.
- Core: ONE RESOURCE -> ONE OWNER -> ONE PREDEFINED AREA -> ONE SERIALIZED WRITE PATH.
- ENFORCEMENT RULE: if the mandatory first read has not been completed in the current work session, STOP. Do not code, change architecture, alter a guardrail, or give consequential Firebase/Google console instructions until the documents have been reread and reconciled with the requested task.
- A green CI result does not override a published architecture rule. If a gate conflicts with an implementation, investigate the implementation first; do not weaken the gate merely to make the new implementation pass.

REBUILD BASELINE — SEPTEMBER 5, 2026
- User authorized forward cleanup/rebuild and asked work to continue autonomously until actual intervention is required.
- Protected working branch: `fidunio-rebuild-baseline-2026-09-05`, created from `main` commit `246c524abc78404d9bc744b272ce08244a35cc3f`. `main` remains rollback/reference.
- Protected checkpoints:
  - `checkpoint-rebuild-baseline-recovery-pass` -> `e2d2e10031182781f6887b2cd1a971701aa21e3a`
  - `checkpoint-rebuild-new-message-owner` -> `b4731926432af3a986af991d2f51db86acea0fd1`
  - `checkpoint-rebuild-account-e2ee-auth-binding` -> `251679cd9240e45f335536fed9bed5bc43e76157`
  - `checkpoint-rebuild-account-dm-v3-crypto-rules` -> `ca9222bdb7171ae60de5b2b9c08bf5b9327f52c9`
- `REBUILD-BASELINE-AUDIT.md` and `RUNTIME-TRANSFORM-INVENTORY.md` are the current keep/remove/next-step authorities.
- Rebuild target is forward consolidation, not historical rollback.

FIRESTORE E2EE V1 GATE — EXACT SCHEMA
- Firebase Local Emulator Suite gate uses demo project `demo-fidunio-e2ee-rules`, never production.
- Exact account identity matrix remains 42 assertions and still passes after account-message rule additions.
- Exact public JWK is `{kty,crv,x,y}` only.
- Exact recovery wrapper fields: `version,ciphertext,iv,wrappedRecoveryKey,wrappingAlgorithm,recoveryAuthorityVersion,recoveryKeyIv,recoveryKeyWrappingAlgorithm`; generic metadata forbidden.
- Repository validation and live deployment are separate gates.
- LIVE RULES DEPLOYMENT COMPLETED SEPTEMBER 6, 2026 using the reviewed GitHub -> verified script -> Firebase workflow.
- Active live ruleset: `projects/fidunio-fef13/rulesets/52ea515e-359f-453f-8822-3c0f6ef2659a`.
- Previous live ruleset: `projects/fidunio-fef13/rulesets/42640d51-c425-4615-bcdf-44d9be990c3f`.
- Future rule changes still require review/test/exact-source pin/deploy/verify; a green repository test alone never changes production.

ACCOUNT E2EE IDENTITY MANAGER — VALIDATED
- `e2ee-account-identity-manager.js` is sole account identity lifecycle owner/state machine.
- iPad/Safari browser gate previously passed ALL 24 TESTS.
- Sign-out generation invalidation prevents stale in-flight work from repopulating runtime identity.
- Ordinary runtime private key is non-extractable.
- `e2ee-account-firestore-adapter.js` receives initialized central Firebase services and never initializes Firebase independently.
- `e2ee-account-firebase-adapter.js` central exposure has a passing CI gate.

ACCOUNT E2EE AUTH BINDING — MATERIALIZED, FAIL-CLOSED
- Commit `ee27f32a46ef4a058a2c1e430f4a3c108e31f260` bound account-E2EE lookup/reset to the existing Firebase auth callback in `app.js`.
- On authenticated UID: `bindAuthenticatedAccountE2EE(uid)` performs serialized authoritative identity lookup.
- On sign-out: `resetAccountE2EEForSignOut()` invalidates in-flight work and clears runtime state.
- This step does NOT enroll, unlock, replace identity, change direct-message transport, or deploy Firebase.
- Missing durable identity remains `EMPTY`; existing identity remains `LOCKED` until explicit password + exact six-digit PIN unlock is wired.
- Protected checkpoint: `checkpoint-rebuild-account-e2ee-auth-binding` -> `251679cd9240e45f335536fed9bed5bc43e76157`.

ACTION_REQUIRED SECURITY-GATE DIAGNOSIS — RESOLVED
- Run `33981660852` (`Rebuild Baseline Security Gate`, run 222) at `ee27f32...` concluded `action_required` with ZERO jobs.
- Cause: the bounded one-shot account-E2EE auth materializer committed/pushed as `github-actions[bot]`, and the follow-up PR workflow was blocked before test execution.
- This was NOT a code/security-test failure.
- The one-shot materializer workflow/script was deleted after completing its job.
- Normal user-authored branch validation returned green; all security steps passed.

E2EE V1 CRYPTO
- iPad Safari crypto validation passed isolated tests.
- Frozen identity wrapper format: ECDH P-256; stable random keyId; transient PKCS#8 only; AES-256-GCM wrappers; PBKDF2-HMAC-SHA256 600000; random 16-byte salt; random 12-byte IV; exact six-digit account E2EE PIN; canonical JSON KDF/AAD; base64url without padding.

ACCOUNT DIRECT-MESSAGE E2EE V3 — VALIDATED REPOSITORY CANDIDATE
- New isolated target direct-message format is `e2ee:3`, `kdfVersion:1`.
- `e2ee-account-message-crypto.js` uses durable account ECDH P-256 identities -> 256-bit shared secret -> HKDF-SHA256 -> non-extractable AES-256-GCM key.
- KDF is bound to conversation ID plus sorted `[uid,keyId]` durable-account pairs. Device ID is absent.
- Message AAD binds `conversationId,messageId,senderUid,recipientUid,senderKeyId,recipientKeyId`.
- Exact crypto envelope fields: `e2ee,kdfVersion,senderKeyId,recipientKeyId,ciphertext,iv`.
- No plaintext, device ID, per-device envelope map, sender device public key, or recipient device list is part of v3 crypto.
- `e2ee-account-message-crypto.test.mjs` passes directional round trips, strict JWK/base64url shape, AAD substitution, key substitution, tamper, empty and Unicode cases.
- `e2ee-account-message-service.js` is fail-closed: it operates only when the local durable account identity is already READY and the peer exact authoritative public account identity is available. It never falls back to per-device E2EE and never generates an identity.
- `e2ee-account-message-service.test.mjs` passes its fail-closed/identity-validation matrix.
- `firestore.rules` contains exact v3 direct-message acceptance tied to authoritative `e2eePublicKeys/{uid}.keyId`, while retaining legacy plaintext/e2ee:1/e2ee:2 compatibility.
- Dedicated `firestore-account-message-v3.rules.test.mjs` found one real cross-format flaw: mixed E2EE+plaintext rows could be accepted by the old plaintext OR branch. Commit `7eb1d22fe1c3323ca613ff986812be5dc02d7b21` closed it by requiring plaintext rows to contain no `e2ee` field.
- After that fix both the original 42-case rules gate and dedicated v3 message matrix passed.
- The reviewed Firestore rules supporting v3 are now LIVE, but v3 is NOT yet wired into normal runtime transport. Do not confuse rules deployment with transport cutover.
- Protected checkpoint: `checkpoint-rebuild-account-dm-v3-crypto-rules`.

RECOVERY SERVER — VALIDATED THREE-COMPONENT IMPLEMENTATION
- Recovery uses random 256-bit RUK; client recovery wrapper + PIN/server-master protected RUK. No plaintext password/PIN/RUK/private key/master secret in Firestore.
- Exact server KDF bytes: UTF8(JSON.stringify(["FIDUNIO-E2EE-RECOVERY-KDF",1,uid,keyId,pin])). HMAC-SHA256 keyed by 32-byte recovery master secret produces AES-256-GCM RUK wrapping key.
- Exact server RUK AAD bytes: UTF8(JSON.stringify(["FIDUNIO-E2EE-RECOVERY-RUK",1,uid,keyId])).
- Recovery session: 10 minutes; max 5 PIN failures/session; max 10 consecutive account PIN failures; new session does not reset account counter; success resets counter.
- Prototype-era `supplementalVerifier()` and supplemental recovery state were explicitly STRICKEN on September 6, 2026. There is no fourth recovery factor.
- Recovery completion now serializes `PENDING -> VERIFYING` in Firestore before PIN/master-secret cryptography. This prevents concurrent completion calls from performing parallel PIN guesses against one session.
- Wrong PIN transitions `VERIFYING -> PENDING` or `LOCKED` while atomically updating counters. Success transitions `VERIFYING -> CONSUMED` and resets account PIN failures before returning the RUK.
- A stranded `VERIFYING` session fails closed; it is not silently reopened.
- Recovery crypto/session/callable/persistence tests pass.
- Full Rebuild Baseline Security Gate run #318 (`34011735357`) passed on commit `f7d764df29fd936d2893645813257ea19deda6c1` after the strengthened Functions scaffold guard.

RECOVERY AUTHORITY — THREE-COMPONENT DESIGN FINAL
- The recovery architecture decision is NOT open and must not be reopened because prototype code once contained a supplemental verifier.
- Component 1: authenticated FIDUNIO/Firebase account establishes account/UID and recovery context.
- Component 2: user's existing exact six-digit FIDUNIO account-E2EE PIN is the user-held cryptographic recovery factor.
- Component 3: separate Google-hosted Cloud Functions recovery authority controls use of the protected server recovery secret in Google Secret Manager.
- There is NO supplemental verifier, security question, extra password, second PIN, test PIN or bypass.
- Browser/client never receives `FIDUNIO_RECOVERY_MASTER_V1`; no single client-side component contains all material required to recover the identity.
- Google-side authority uses the server master secret only inside the reviewed recovery operation to unwrap the RUK; it does not store or expose the account private key in plaintext.
- Successful recovery restores the SAME durable account E2EE private identity/keyId. Failure never silently creates a replacement identity.
- Firebase App Check is an additional callable abuse-defense when production enforcement is later enabled. App Check enforcement remains OFF during staging and recovery deployment/testing.

CLOUD FUNCTIONS RECOVERY — REVIEWED SOURCE READY, NOT YET DEPLOYED
- Authoritative server source is under `functions/recovery/`; root recovery modules are compatibility re-exports so tests exercise deployable source.
- `functions/package.json`: Node.js 22, `firebase-functions` 7.3.2, `firebase-admin` 14.3.0.
- `firebase.json` registers Functions codebase `recovery` with source `functions`.
- `functions/index.mjs` exports v2 callables `enrollRecoveryV1`, `startE2EERecoveryV1`, `completeE2EERecoveryV1`.
- All three callables use dedicated runtime service account `fidunio-recovery@fidunio-fef13.iam.gserviceaccount.com`.
- `FIDUNIO_RECOVERY_MASTER_V1` is declared via `defineSecret`; enrollment/completion bind it, start does not.
- Secret format: exactly 32 random bytes, base64url without padding.
- `completeE2EERecoveryV1` is wired to the reviewed final recovery core; the obsolete fail-closed supplemental-verifier stub is gone.
- Functions staging source explicitly sets App Check enforcement OFF (`REQUIRE_APP_CHECK = false`). Limited-use completion-token consumption is deferred until App Check itself is proven and deliberately enabled.
- All three Recovery Functions were deployed and verified ACTIVE in us-central1 on September 6, 2026.
- Before deployment, verify the dedicated recovery runtime service account has the minimum required Firestore data-access IAM. Secret Accessor on the recovery secret is already configured, but Firestore runtime permission is not yet independently proven in the durable handoff record.

DETERMINISTIC UI / CURRENT RUNTIME — APP CHECK AUTHORITY REPAIRED
- `bootstrap.js` is startup sequencing only; no broad bootstrap scrub observer.
- Raw `app.js` starts from empty production state and explicitly owns Messages/Chat/Settings/New Message/group structural UI.
- `new-message-owner.js` owns its recipient-picker region explicitly; observer-based New Message polish deleted.
- Group metadata/subscription/auth lifecycle + real creation UI are fully materialized in raw source; group message send remains fail-closed pending group E2EE.
- Peer display-name synchronization and main Sign Out are explicit through central `firebase.js`; former `profile-sync.js` and `main-screen-polish.js` deleted.
- Settings lifecycle is explicitly mounted by `app.js`; former bridge deleted.
- `firebase.js` is again the sole runtime Firebase SDK/service owner, INCLUDING App Check.
- `firebase-app-check.js` second owner was deleted September 6, 2026.
- `auth-ui-clean.js` no longer starts an independent App Check lifecycle.
- `firebase-app-check.test.mjs` validates central initialization order: one app -> App Check -> Auth/Firestore.
- `runtime-authority-gate.test.mjs` again permits exactly one Firebase SDK owner.
- Full security-gate run #316 (`34011694149`) and final run #318 (`34011735357`) passed after this repair.

SERVICE WORKER / HIDDEN DUAL CODE
- `service-worker.js` still rewrites `app.js`, but only legacy/per-device E2EE compatibility transforms remain:
  1. per-device E2EE v2 helpers;
  2. v2 receive/decrypt branch;
  3. legacy trust-gate transform;
  4. Outbox send conversion to per-device fan-out.
- These MUST NOT be copied forward as target architecture.
- Do not remove them until a real authenticated account can safely reach durable account E2EE READY and v3 send/receive/Outbox is validated.
- Final service worker must perform shell/network/offline caching only and never alter JavaScript semantics.

OFFLINE MESSAGING TARGET
- Firestore = durable encrypted authority.
- UID-scoped local cache = rebuildable offline copy.
- Outbox = temporary pending-send authority.
- Reconnect must be serialized/idempotent with stable message IDs.
- Existing offline/receipt behavior is protected during migration.

LIVE FIREBASE / GOOGLE HANDOFF — ACTUAL CURRENT STATE
- Live project: Firebase `FIDUNIO`, project ID `fidunio-fef13`, project number `130339622893`.
- Firestore `(default)`: Native mode, location `nam5 (United States)`.
- Billing upgraded to Blaze; owner configured a project budget alert separately.
- reCAPTCHA Enterprise API enabled.
- reCAPTCHA Enterprise Web key registered for `willyros01.github.io`.
- Firebase App Check Web app registered with reCAPTCHA Enterprise; token TTL remains default 1 hour.
- App Check enforcement remains OFF.
- Recovery master secret `FIDUNIO_RECOVERY_MASTER_V1` exists; version 1 enabled; value was never displayed.
- Dedicated recovery service account exists: `fidunio-recovery@fidunio-fef13.iam.gserviceaccount.com`.
- Secret Accessor is granted narrowly on the recovery secret, not project-wide.
- Functions region is settled: `us-central1`.
- Reviewed Firestore rules are deployed and verified. Active ruleset: `projects/fidunio-fef13/rulesets/52ea515e-359f-453f-8822-3c0f6ef2659a`.
- All three Recovery Functions are deployed and verified ACTIVE.
- No normal direct-message v3 transport cutover.
- Normal account identity enrollment still cannot be enabled safely until recovery enrollment is live and proven, because identity creation establishes both normal and recovery wrappers atomically.
- Existing durable identity lookup/unlock must use the reviewed account-E2EE Firestore permissions in the actual project.
- Normal direct-message transport must not switch to v3 until a real authenticated account can reach E2EE READY without silent identity replacement.
- Remaining per-device transforms stay until the live account-E2EE/recovery boundary is proven.
- Next live step: one controlled Recovery Functions deployment with exact-source pin, minimum-runtime-IAM preflight and post-deploy verification.
- The deployment preflight must verify the dedicated recovery service account's minimum Firestore data-access role; do not fall back to a broad default runtime identity.

PROCESS-INTEGRITY DEVIATION — SEPTEMBER 5, 2026 — RECTIFIED SEPTEMBER 6
- The user explicitly required the mandatory repo documents to be reread before starting work and had previously been assured that safeguard was in place.
- At the start of the later App Check continuation, the assistant did NOT perform the mandatory repository-first reread and relied on conversational carry-over instead. This violated the first rule in this file and `CODING-GUIDELINES.md`.
- Audit identified these gaps created by that deviation:
  1. `firebase-app-check.js` introduced as a second direct Firebase SDK owner contrary to the published one-owner Firebase authority.
  2. `runtime-authority-gate.test.mjs` was changed to allow the second owner instead of requiring the implementation to conform to the existing authority rule.
  3. App Check initialization was placed after `initFirebase()`/first auth callback instead of inside the single central Firebase initialization lifecycle.
  4. App Check provider registration progressed before the already-decided three-component recovery architecture had been correctly recovered into durable documentation.
  5. Durable handoff docs were not updated immediately after live billing/App Check progress, leaving stale text that later led to attempted repeated work.
  6. A temporary bad `auth-ui-clean.js` edit occurred during the continuation and was removed by resetting the rebuild branch to a known prior commit.
  7. The three-component recovery decision was not persisted when originally instructed, later causing it to be incorrectly described as unresolved.
  8. Prototype-era recovery scaffold contained a supplemental verifier and related state even though the final design had no fourth factor.
- Rectification completed September 6:
  1. mandatory repository-first reread was performed before this repair session;
  2. supplemental verifier/state permanently removed from recovery source/tests/docs;
  3. recovery completion implemented against the final three-component design;
  4. a parallel PIN-guess race was found during review and fixed with serialized Firestore `PENDING -> VERIFYING` reservation before cryptography;
  5. `firebase.js` restored as sole Firebase SDK/service owner including App Check;
  6. `firebase-app-check.js` deleted;
  7. auth gate no longer creates a second App Check lifecycle;
  8. strict single-owner runtime authority gate restored;
  9. App Check tests strengthened to enforce central ordering;
  10. Functions scaffold gate strengthened to reject supplemental-verifier regression, require staging App Check OFF, dedicated runtime SA and real completion wiring;
  11. full security-gate runs #316 and #318 passed after these repairs.
- No live compromise identified. App Check enforcement remained OFF throughout. No Recovery Function has yet been deployed. Firestore rules, secret and recovery-secret IAM were deliberately completed separately and are recorded above.

CURRENT REBUILD ORDER — SEPTEMBER 6, 2026
1. COMPLETE — crypto/identity-manager/Firestore/recovery security proving.
2. COMPLETE — initial dead-code cleanup and deterministic UI-owner consolidation.
3. COMPLETE — account E2EE auth lookup/reset binding without transport cutover.
4. COMPLETE — account direct-message v3 crypto + exact rules + fail-closed service validated.
5. COMPLETE — recovery architecture finalized as exactly three components.
6. COMPLETE — Secret Manager recovery master + dedicated recovery SA + secret-level IAM + `us-central1` decision.
7. COMPLETE — reviewed Firestore rules deployed and verified live.
8. COMPLETE — obsolete supplemental recovery verifier/state removed; completion core implemented; concurrent PIN-guess race fixed with VERIFYING reservation.
9. COMPLETE — App Check second-owner defect repaired; `firebase.js` sole owner restored; second owner deleted; authority gate restored.
10. COMPLETE — full security-gate run #318 passed on strengthened current source.
11. CURRENT — final durable documentation reconciliation, then controlled Recovery Functions deployment with minimum Firestore runtime-IAM preflight and post-deploy verification. App Check enforcement stays OFF.
12. AFTER DEPLOYMENT — controlled recovery proof that same durable identity/keyId/history is restored; no replacement identity.
13. AFTER LIVE RECOVERY PROOF — wire explicit six-digit account-E2EE enrollment/unlock readiness into auth/PIN lifecycle.
14. AFTER READY PROOF — migrate direct send/receive/Outbox to v3 while retaining explicit e2ee:1/2 readability and receipts.
15. Remove only matching per-device service-worker transforms after replacement passes; reduce SW to cache/offline only.
16. Finish Firestore-authoritative encrypted history + UID-scoped cache + serialized Outbox.
17. Revalidate iPhone/iPad/PWA receipts/offline/Settings/account switching/reinstall/recovery/local PIN.
18. Only after legitimate App Check traffic is proven should enforcement be considered.
19. Only then decide whether to merge/advance `main`.

PIN / LOCAL SECURITY
- Existing installation-local PIN remains 4–12 digits under current local-security design.
- New account E2EE PIN is exactly six digits.
- Reconcile deliberately later; do not conflate them accidentally.
- Remove Local PIN bug remains open.

PROTECTED BASELINES / CONVENTIONS
- Preserve 0.9.4.11 stable behavior, 0.9.5.1 Settings, 0.9.5.4 receipts, 0.9.5.7 legacy identity race stabilization.
- Rejected 0.9.4.12-.15 and rejected invite-install work remain forbidden.
- Hermes internal/FIDUNIO public; one cumulative root `hermes-memory.txt`; one reusable root `hermes-setup.txt`.
- Never overwrite/regenerate `firebase-config.js` or `config-firestore.js`.
- `version.js` authoritative; preserve artwork/navy-teal-gray/no gold/accessibility/display-name UX and iPhone/iPad behavior.

FINAL HANDOFF RULE
Read supporting repo docs before code. Repository docs are durable authority. DETERMINISTIC: reduce competing authorities; do not add repair machinery. Never remove the last working transport/recovery path before its account-authoritative replacement is proven.

RECOVERY FUNCTIONS LIVE VERIFICATION — SEPTEMBER 6, 2026
- enrollRecoveryV1, startE2EERecoveryV1, and completeE2EERecoveryV1 are ACTIVE in us-central1.
- All three use fidunio-recovery@fidunio-fef13.iam.gserviceaccount.com.
- FIDUNIO_RECOVERY_MASTER_V1 is bound only to enrollment and completion; start has no secret binding.
- roles/datastore.user is present on the dedicated recovery runtime service account; Secret Accessor remains narrow on the recovery secret.
- Artifact Registry gcf-artifacts cleanup policy is configured to delete images older than 1 day.
- App Check enforcement remains OFF. Firestore rules, Hosting, Auth, and normal message transport were not changed by final verification.
- The first deploy returned an error only because artifact cleanup policy was not yet configured; the three Functions themselves reported successful create operations. The final verification script then confirmed ACTIVE/runtime-SA/secret bindings and set cleanup policy successfully.
- Console/Cloud Shell setup is complete. Do not repeat deployment or console setup. The next boundary is client recovery/enrollment validation and account-authoritative runtime migration.

ACCOUNT E2EE CLIENT/RUNTIME CUTOVER — SEPTEMBER 6, 2026
- Recovery callable client is centralized through firebase.js Functions SDK in us-central1; Firebase Auth/App Check tokens are attached by the official callable SDK.
- Settings has a named Account Encryption panel for enrollment, unlock, and post-password-reset recovery. The account E2EE PIN remains exactly six digits and separate from the installation-local 4–12 digit app-lock PIN.
- Firebase password change is coordinated with account E2EE rewrap and attempts rollback if Auth update fails.
- Raw app.js now sends/decrypts direct-message e2ee:3 via the account-authoritative message service. New sends fail closed unless the durable account identity is READY.
- service-worker.js no longer transforms app.js or owns E2EE semantics; it is cache/transport only. Legacy e2ee:1/e2ee:2 receive compatibility remains in raw app.js for history.
- App Check enforcement remains OFF pending real legitimate-device validation and metrics.

V3 REPOSITORY CHECKPOINT — SECURITY GATE GREEN — SEPTEMBER 6, 2026
- Version 0.9.6.3 is the branch test checkpoint.
- Full Rebuild Baseline Security Gate passed all substantive steps after the account-E2EE runtime cutover: Firestore E2EE rules, account-message rules, recovery crypto/session/callable/Firestore adapter, central Firebase adapter, account auth lifecycle, recovery client boundary, direct-message crypto/service, App Check integration, Functions scaffold, transform gate, and runtime authority gate.
- Recovery Functions remain live/ACTIVE from the verified console handoff; this repository work did not redeploy them.
- Account Encryption UI, callable recovery client, coordinated password rewrap, e2ee:3 direct send/receive, Outbox send path, and service-worker transform retirement are materialized on fidunio-rebuild-baseline-2026-09-05.
- App Check enforcement remains OFF.
- STOP BOUNDARY: do not merge/deploy this v3 transport to the live app until a legitimate signed-in account performs the first real Account Encryption enrollment/unlock/recovery proof on a test device. That proof necessarily needs the user's authenticated browser and chosen six-digit account E2EE PIN; it cannot be completed by repository CI without exposing credentials or fabricating a user identity.
- Required first live proof: enroll -> READY -> record stable keyId -> sign out/reset runtime -> unlock same keyId -> controlled recovery after password-reset test -> same keyId/history -> two-device e2ee:3 send/read/offline-Outbox test. Only after that proof may the branch be merged/deployed and later App Check metrics evaluated for enforcement.

RELEASE ROADMAP — FIRST REBUILD / 1.1 / 1.2
- User explicitly set release scope on September 6, 2026.
- First complete rebuild release does NOT require Firebase Cloud Messaging (FCM) to be active. FCM notification implementation/activation is deferred to FIDUNIO 1.1 and must not block the first rebuild release.
- First complete rebuild release does NOT require Firebase App Check production enforcement to be active. App Check production activation/enforcement is deferred to FIDUNIO 1.2 and must not block the first rebuild release.
- Existing centralized App Check client ownership/configuration may remain present with enforcement OFF.
- FCM remains a documented roadmap requirement for 1.1: privacy-preserving notification/wake transport only, Firestore remains message authority, no plaintext message/attachment content in push payloads by default, multi-endpoint UID support, and non-FCM/Fire OS fallback.
- App Check 1.2 must be enabled only after legitimate supported client/recovery flows are validated and rollout testing proves no legitimate-client lockout.
- The first-rebuild completion percentage and final release gate exclude FCM 1.1 and App Check 1.2 work.

2026-09-06 — GROUP ADMINISTRATION MATERIALIZED
- Real cloud-backed Group Info rename/add/remove/non-owner leave implemented under deterministic ownership.
- Membership changes are atomic with E2EE epoch rotation: post-change membership/admin arrays, member doc mutation, `keyEpoch + 1`, and the new epoch record are one Firestore transaction/batch-equivalent authority path in `firebase.js`.
- `e2ee-account-group-runtime.js` prepares replacement epoch envelopes for exactly the post-change members; removed/leaving accounts receive no new envelope.
- Firestore Rules require a matching post-state epoch and exact epoch map keys for every membership change; owner removal/leave is denied.
- Default `historyPolicy: fromJoin` remains authoritative. Explicit earlier-history grants/admin-role transfer remain unfinished and must not be simulated.
- Group Info now reads admin/history metadata from cloud snapshots. Search/shared-files/security controls remain separate placeholders and are not part of this completed administration slice.

2026-09-06 — GROUP ADMINISTRATION REPOSITORY VALIDATION GREEN
- Materialized group administration commit: `c62db46bc8646f68beb54fd5038ae0d38bac1170`.
- Focused materializer validation passed group runtime, group service/controller/integration ownership, Firestore group E2EE rules, and runtime authority tests.
- Full Rebuild Baseline Security Gate run `34045259037` then passed every substantive step on the cleaned authoritative branch after temporary group-administration workflows were removed.
- Group rename/add/remove/non-owner leave are therefore repository-validated DONE. Explicit earlier-history grants/admin-role controls remain IN PROGRESS; no `htest` deployment was performed.


2026-09-06 — GROUP EARLIER-HISTORY GRANT FOUNDATION
- User approved administrator-selected starting point/date for explicit earlier-history sharing; Beginning of conversation is a valid choice and resolves to the earliest still-retained eligible message.
- Security boundary: an exact date/message grant must not disclose a whole historical epoch key when that would expose older messages.
- Added isolated `e2ee-account-group-history-crypto.js`: account ECDH P-256 + HKDF-SHA256 + AES-256-GCM re-encrypts one selected source message to one target account. Grant/group/source-message/grantor/target identities are cryptographically bound; device IDs are absent.
- Added dedicated crypto test and security-gate step. This is foundation only: Firestore grant schema/rules, runtime/service/transport integration, Group Info control, disappearing-content purge linkage and emulator tests remain required.
- Version checkpoint advanced to 0.9.6.5.
- Overall first complete rebuild estimate remains approximately 65%; do not inflate it for this isolated foundation.
- Reporting rule reinforced by user: after every substantive build run report the overall rebuild percentage even when unchanged, and reconcile all affected durable docs.


2026-09-06 — GROUP ADMINISTRATION BRIDGE REPAIR / 0.9.6.6
- Before extending earlier-history persistence, repository-first source audit found a concrete bounded regression in `e2ee-account-group-app-integration.js`: its four Group Info administration wrappers referenced controller delegates that were not imported.
- Repaired the import surface without changing ownership: app integration -> app controller -> group service/runtime -> Firebase adapter -> firebase.js remains authoritative.
- Strengthened `e2ee-account-group-app-integration.test.mjs` so rename/add/remove/leave must all be imported from the controller and delegated through the bounded bridge.
- Version checkpoint advanced to 0.9.6.6.
- The prior full Rebuild Baseline Security Gate run `34046083575` completed green including the new group-history grant crypto test. A fresh gate is required for this wiring repair before treating the checkpoint as validated.
- Earlier-history Firestore schema/rules/runtime/UI/purge linkage remain unfinished; do not enable the Group Info history-sharing control yet.
- Overall first complete rebuild estimate remains approximately 65%.


2026-09-06 — README / RELEASE-HISTORY LEDGER RULE
- User requested README become the cumulative human-readable build history, including significant milestones, protected checkpoints, rejected builds and rollback/reset history.
- README was modernized at 0.9.6.6 to cover the early prototype through the current complete rebuild, including the rejected 0.9.4.12-.15 invite/install line and return to the stable 0.9.4.10/0.9.4.11 foundation, three-component recovery correction, duplicate App Check/Firebase owner correction, service-worker semantic-transform retirement, current group/history work, and major green security-gate evidence.
- Durable rule: whenever `version.js` is incremented, reset, rolled back or reassigned, README MUST be updated in the same work session with old/new version, reason, significant changes, rollback/rejection status, validation evidence and follow-on constraints.
- The same version-change boundary must reconcile `hermes-memory.txt`, `FIDUNIO-BUILD-CHECKLIST.md` and all additional affected docs.
- Keep one cumulative root README; do not create version-numbered README files.
- This documentation-only change does not alter release number 0.9.6.6 or the approximately 65% first-rebuild completion estimate.


2026-09-06 — GROUP HISTORY PERSISTENCE FOUNDATION / 0.9.6.7
- Confirmed prior cleaned-branch Rebuild Baseline Security Gate run `34046696823` green before starting this slice.
- Implemented exact repository Firestore schema/rules for explicit earlier-history grant metadata plus per-source-message encrypted copies. Default `fromJoin` remains unchanged.
- Grant construction is two-phase (`building` -> `active`) so large Beginning-of-conversation grants can be written in chunks without exposing partial history. Target reads are denied until activation.
- Rules require current admin grantor, another current member as target, authoritative durable account keyIds, retained e2ee:4 source messages, source timestamp equality and lower-bound enforcement.
- `firebase.js` remains sole Firebase owner and now provides begin/write-chunks/activate/read APIs for opaque grant records.
- `e2ee-account-group-runtime.js` serializes grant creation, decrypts only already-authorized source messages, re-encrypts each through the isolated history crypto owner, and decrypts active grants for the target account.
- Runtime and Firestore group E2EE tests were expanded.
- Version advanced 0.9.6.6 -> 0.9.6.7 and README updated in the same release boundary per the release-ledger rule.
- This is not the completed UI feature: Group Info date selection, granted-history conversation projection, disappearing-content physical purge of grant copies/local plaintext and anti-resurrection remain required.
- Repository rules now contain history-grant policy but LIVE Firebase rules were NOT deployed; do not deploy until the controlled Firebase handoff.
- htest was not touched. App Check enforcement remains OFF. FCM remains deferred to 1.1.
- Overall first complete rebuild estimate remains approximately 65%.


2026-09-06 — 0.9.6.7 GROUP HISTORY PERSISTENCE VALIDATION GREEN
- Materialized persistence/runtime foundation commit: `3c43c60834f95848b1f1cb5141b88e5efa2704d4`.
- Review caught a test-only chronology fixture defect before final validation: valid group message test incorrectly used a fixed `createdAt` although rules require request time, and the history-grant matrix executed after removing its target member. The fixture was corrected to capture the authoritative server timestamp and restore target membership under disabled rules for the independent history-policy matrix.
- A newly created branch-only one-shot workflow also produced zero-job/failure noise because GitHub had not registered it for same-push execution. This was workflow-registration noise, not a security/application test failure. The actual bounded materializer/repair ran through registered workflows and succeeded.
- All temporary group-history workflows, scripts and trigger files were removed before the final clean validation.
- Cleaned-branch Rebuild Baseline Security Gate run `34047570212` PASSED all substantive steps, including the expanded group E2EE/history-grant rule matrix and group history crypto/runtime integration gates.
- 0.9.6.7 therefore has a repository-validated earlier-history persistence/runtime foundation. Do NOT mark the user-facing feature DONE: Group Info date selection, granted-history conversation projection, disappearing-content physical purge/anti-resurrection, and real-app/device proof remain unfinished.
- Repository Firestore rules changed, but LIVE Firebase rules were NOT deployed. htest was NOT touched. App Check enforcement remains OFF; FCM remains deferred to 1.1.
- Overall first complete rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.8 GRANTED GROUP HISTORY PROJECTION
- `e2ee-account-group-history-projection.js` is a pure deterministic projection helper; it owns no Firebase, crypto or mutable storage.
- `e2ee-account-group-conversation.js` now asks the existing group service/runtime for active granted-history plaintext and merges it into the normal conversation projection.
- An ordinary row that decrypts normally always remains authoritative. A grant can replace only the undecryptable body for the same source message ID; duplicates are removed and ordering is deterministic by source time/message ID.
- Normal group Delivered/Read receipts are written only when the ordinary group ciphertext decrypted successfully. Earlier-history grant copies do not create retroactive normal-message receipts.
- Conversation projection tests execute the merge behavior rather than checking only source tokens. Rebuild Baseline Security Gate run `34048452887` PASSED all substantive steps on implementation commit `07c274e80d5c04e18ef831f1c9f53d7da3a28f19`.
- History sharing is still NOT user-facing DONE. Group Info date/Beginning selection + real grant invocation, physical purge of source/grant/local copies, anti-resurrection, and final device validation remain required.
- Version advanced 0.9.6.7 -> 0.9.6.8. No live Firebase deployment, no htest deployment, no FCM activation, no App Check enforcement. Overall first-rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.9 GROUP HISTORY SOURCE AUTHORITY
- Production earlier-history grants no longer accept source message rows from app.js, local cache, or other callers.
- e2ee-account-group-runtime.js requests retained encrypted source rows from `readRetainedGroupMessages`; e2ee-account-group-firebase-adapter.js implements that as a one-shot view over the existing firebase.js group-message subscription and ignores cache-only snapshots until a server-backed snapshot arrives.
- This preserves firebase.js as sole SDK owner while preventing stale/tampered UI projection from becoming cryptographic authority.
- Approved Beginning/date boundaries are filtered inside the serialized runtime against those authoritative retained rows. Runtime tests inject forged caller `sourceRows` and prove they cannot affect source selection or boundary decisions.
- Production grant IDs are runtime-owned via secure `crypto.randomUUID()`.
- e2ee-account-group-app-controller.js and app-integration now expose an intent-only history grant path (`groupId`, `targetUid`, `boundary`) but app.js does not yet invoke it. This is deliberate: user-facing activation remains blocked on disappearing-content physical purge/anti-resurrection.
- Rebuild Baseline Security Gate run `34048905157` PASSED all substantive steps.
- Version advanced 0.9.6.8 -> 0.9.6.9. Live Firebase, htest, FCM and App Check enforcement remain untouched. Overall first-rebuild estimate remains approximately 65%.

2026-09-06 — DISAPPEARING CONTENT READ-TIMER SEMANTICS
- User selected fixed-time-after-read semantics for both individual/direct and group disappearing messages.
- Direct message: the recipient account's first authoritative Read event starts that recipient's disappearance interval.
- Group message: each recipient account has its own independent first-Read timestamp and expiry window. One member reading does NOT start the timer for the other group members.
- The read/expiry authority must be server-backed and account-scoped; a device-local wall clock or UI-only read event is not sufficient authority.
- At a recipient's expiry, that recipient must no longer retain or project FIDUNIO-controlled local/cache/plaintext/grant copies of the content.
- Because a group message has one shared Firestore source ciphertext, the shared source record can be physically purged only after all still-authorized recipient expiry windows that apply to that source have elapsed (or a recipient is otherwise no longer entitled to the message). Final purge must also delete receipts, history-grant copies/references, attachment traces, local caches/Outbox remnants, and prevent stale-device resurrection.
- No per-message tombstone or retained expired:true source record is allowed after final purge.
- This decision resolves the timer-start ambiguity but does not mark disappearing content DONE. Exact metadata schema, authoritative timestamp write path, rules, purge owner, offline convergence and tests remain to be implemented.
- Overall first-rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.10 IMMUTABLE FIRST-READ AUTHORITY
- `disappearing-content-policy.js` is the pure policy owner for recipient expiry and group shared-source purge eligibility; it owns no Firebase, storage, DOM, crypto or deletion writes.
- Direct receipt authority is now serialized in `firebase.js`: only the non-sender recipient may advance Sent -> Delivered or first Sent/Delivered -> Read; first Read writes server-backed `readAt`, and repeat Read returns the existing timestamp without moving the clock.
- Group receipt authority is per recipient UID. Delivered records contain `uid,state,updatedAt`; Read records additionally contain immutable server-backed `readAt`. One member cannot write another member's receipt or start another member's timer.
- Firestore Rules bind first `readAt` to `request.time`, deny sender-written direct receipt authority, deny repeat Read timestamp movement, and preserve ciphertext/message immutability during receipt transitions.
- `markCloudConversationRead()` now delegates through the same authoritative direct receipt transaction instead of writing state independently.
- Added `disappearing-read-authority.test.mjs`; direct and group emulator matrices now prove first-Read timestamp authority and repeat-Read denial. These gates are included in the normal Rebuild Baseline Security Gate.
- Rebuild Baseline Security Gate run `34050343201` passed on commit `2ca9c61001a4d7a3f494800256dbf26277e28106` after the 0.9.6.10 README checkpoint.
- Repository Firestore rules changed but were NOT deployed to live Firebase. htest was not touched. App Check enforcement remains OFF; FCM remains deferred to 1.1.
- This checkpoint does not physically purge content. One serialized purge/delete owner, history-grant purge, local/offline convergence, attachments and stale-device anti-resurrection remain unfinished.
- Overall first complete rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.11 USER-SELECTED DISAPPEARING DURATION
- User explicitly decided that the disappearing duration is user-selected; FIDUNIO must not impose one universal duration.
- `disappearing-content-policy.js` remains the pure policy owner and now defines `DISAPPEARING_DURATION_FIELD = "disappearAfterSeconds"`.
- Off/non-disappearing is represented by no duration field. A selected duration is an exact integer number of seconds, currently bounded to 1..31536000 (one year).
- Each disappearing message must eventually persist the resolved duration immutably. UI presets or conversation defaults may assist selection, but later preference changes must not modify the duration of a message already sent.
- Expiry authority remains recipient first server-backed Read (`readAt`) from 0.9.6.10; group recipients retain independent clocks.
- 0.9.6.11 adds policy/test coverage only. Direct/group Firestore message schema persistence and rules for `disappearAfterSeconds`, purge execution, local/offline anti-resurrection, attachments, and final UI remain IN PROGRESS.
- Live Firebase and htest remain untouched. App Check enforcement remains OFF; FCM remains deferred to 1.1.
- Overall first complete rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.12 IMMUTABLE OUTER DISAPPEARING METADATA
- User-selected duration is now threaded through current direct/group send authorities as optional outer `disappearAfterSeconds` message metadata, never inside E2EE v3/v4 crypto envelopes.
- `firebase.js` remains sole Firebase writer and normalizes the metadata through the pure disappearing policy owner before Firestore persistence.
- Direct encrypted Outbox payloads retain the resolved duration through reconnect. Group encrypted Outbox -> controller -> service -> serialized runtime -> Firebase adapter preserves the same resolved duration while current epoch revalidation may re-encrypt only ciphertext.
- Firestore rules allow the optional integer only in range 1..31536000 seconds. Existing receipt update diff rules make duration immutable after create.
- Direct/group emulator tests and `disappearing-message-metadata-authority.test.mjs` cover the new boundary; crypto modules remain exact and unaware of disappearing metadata.
- Physical trace-free purge, server purge authority, grant-copy/attachment purge, local/offline convergence and stale-client anti-resurrection remain unfinished and must be completed before exposing disappearing controls.
- No live Firebase or htest deployment; App Check enforcement remains OFF; FCM deferred to 1.1.
- Overall first complete rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.13 DETERMINISTIC PURGE-ELIGIBILITY POLICY
- Clean 0.9.6.12 security gate run `34052064381` passed every baseline step, including the new disappearing metadata authority gate.
- Added pure `disappearing-purge-policy.js`; it does not own Firebase/Admin SDK/storage/DOM/timers/deletes. Production callers must inject server-side current time; device time cannot authorize cloud deletion.
- Direct eligibility: disappearing enabled + recipient state Read + immutable server-backed `readAt` + resolved duration elapsed.
- Group original recipients are the source epoch member UIDs minus sender. Applicable recipients are those original recipients who remain currently entitled. New members outside the source epoch do not retroactively block purge; removed members no longer block it.
- Every applicable original recipient must have an authoritative Read whose independent window has elapsed. Unread applicable recipients block final source purge.
- History-grant targets/copies do not extend source lifetime; source purge must remove subordinate grant copies/references.
- This does NOT execute deletion. Dedicated serialized server purge authority, physical Firestore/subcollection/grant/attachment deletion, local cache purge, reconnect rejection and anti-resurrection remain unfinished.
- No live Firebase/htest change. App Check enforcement OFF; FCM deferred 1.1. Overall first rebuild remains approximately 65%.

2026-09-06 — 0.9.6.14 SERIALIZED PURGE EXECUTOR AUTHORITY
- Version advanced 0.9.6.13 -> 0.9.6.14.
- Added binding `DISAPPEARING-PURGE-AUTHORITY.md` defining one server purge owner, one revalidated trace set, one serialized commit path, no client delete rules, no per-message tombstones, and no device-clock cloud deletion authority.
- Added `disappearing-purge-executor.js` as the single serialized coordination owner. It imports the pure eligibility policy but owns no Firebase/Admin SDK, storage, UI, crypto, or physical deletes.
- The executor requires an explicit server-time provider and an opaque repository `basis`; the future mutable repository must re-read/revalidate that basis inside its transaction/precondition commit path.
- Ineligible direct/group sources never invoke the delete owner. A stale-basis commit fails closed and is not retried from stale state. One executor instance serializes direct and group attempts; cross-instance safety still belongs to the server repository transaction/precondition layer.
- Dedicated executor tests cover eligible direct commit, unread retention, group per-recipient blocking, removed-recipient handling, stale-basis failure, serialization, and explicit server-time authority; this test is now part of the normal Rebuild Baseline Security Gate.
- No Firestore/Storage delete implementation, scheduled Function, delete rule, local purge, attachment purge, live Firebase deployment, or htest deployment is introduced by this checkpoint.
- Next secure implementation boundary: dedicated server repository/adapter for authoritative state reads + revalidated physical trace deletion, then UID-scoped local cache/Outbox anti-resurrection convergence and attachment cleanup.
- Overall first complete rebuild estimate remains approximately 65%.

2026-09-06 — 0.9.6.15 SERVER FIRESTORE PURGE REPOSITORY FOUNDATION
- Clean 0.9.6.14 gate `34053227630` passed all steps before this slice.
- Added server-only `disappearing-purge-firestore-admin-adapter.mjs`; Admin Firestore is injected and no competing Firebase SDK initializer/import was introduced.
- Direct read authority binds conversation + message Firestore update times into an opaque basis and derives the non-sender recipient from the authoritative direct membership. Direct commit re-reads both in a Firestore transaction; unchanged basis deletes the source physically, stale basis fails closed, already-absent is idempotent success.
- Group read authority binds group/message/source-epoch/per-account receipt update times, source-epoch member UIDs and current entitlement UIDs for the existing pure group policy.
- Group physical delete is intentionally blocked with `GROUP_PURGE_TRACE_DELETE_NOT_READY` until history-grant copy/reference cleanup and receipt deletion are materialized together. Do not weaken this fail-closed boundary merely to make group purge appear complete.
- Dedicated adapter tests and normal security-gate step pass on implementation gate run `34053462019`.
- Version advanced 0.9.6.14 -> 0.9.6.15. No scheduler/deployment/client delete rules/live Firebase/htest changes.
- Next: group subordinate trace plan/reconciliation, then local cache/Outbox anti-resurrection and attachment lifecycle integration.
- Overall first complete rebuild remains approximately 65%.

2026-09-06 — 0.9.6.16 GROUP HISTORY-GRANT PURGE TRACE PLANNING
- Clean 0.9.6.15 gate `34053702432` passed before this slice.
- Added pure `disappearing-group-grant-trace-plan.js`: for a disappearing group source it deterministically plans matching grant-copy deletion, whole-grant deletion when no copies remain, or metadata reconciliation (`totalCopies`, earliest retained `firstSharedMessageId`/`firstSharedAt`) when other retained copies remain.
- Planner fails closed on metadata/copy count mismatch, cross-group/cross-grant copy authority, duplicate source copies, or invalid source times. Incomplete building grants therefore cannot be ignored during purge.
- Server Firestore purge repository now includes history-grant docs + copies in authoritative group read state and in the opaque update-time purge basis; the existing pure purge eligibility owner remains unchanged.
- Group physical delete intentionally remains `GROUP_PURGE_TRACE_DELETE_NOT_READY`. Do not bypass this until concurrent grant creation is made basis-visible and the final one-path receipt/grant/source commit is validated.
- Dedicated planner/repository tests are permanent security-gate steps. Gate `34054137154` passed all substantive steps.
- Version advanced 0.9.6.15 -> 0.9.6.16. No live Firebase/htest/App Check/FCM change.
- Next: history-grant creation barrier through group authority update-time, then atomic/revalidated group trace commit, followed by UID-scoped local cache/Outbox anti-resurrection.
- Overall first complete rebuild remains approximately 65%.

2026-09-06 — 0.9.6.17 HISTORY-GRANT PURGE BARRIER
- Clean gate `34054522196` passed the complete baseline including the dedicated grant-barrier and updated group emulator tests.
- `beginCloudGroupHistoryGrant` now uses one Firestore transaction and one `serverTimestamp()` to update group `updatedAt` and create a new building grant together.
- Firestore Rules require `historyGrantBarrier(groupId)`: the same request must change only group `updatedAt` and set it to `request.time`; standalone history-grant creation is denied.
- The group purge basis already includes the group document Firestore update time. Therefore a concurrent new grant is now basis-visible and cannot appear as an unobserved subordinate trace after purge planning.
- Existing matching building-grant retry remains idempotent and does not touch group authority again.
- No group source deletion is enabled yet; `GROUP_PURGE_TRACE_DELETE_NOT_READY` remains intentional until one revalidated receipt/grant/source commit is materialized.
- Version advanced 0.9.6.16 -> 0.9.6.17. Repository rules changed only; live Firebase/htest untouched; App Check OFF; FCM deferred.
- Next: atomic/revalidated group physical trace commit, then UID-scoped local cache/Outbox anti-resurrection and attachment integration.
- Overall first complete rebuild remains approximately 65%.

2026-09-06 — 0.9.6.18 HISTORY-COPY PURGE BARRIER
- Clean gate `34054991773` passed all baseline steps, including new grant-copy barrier coverage.
- `writeCloudGroupHistoryGrantCopies` now shares one server timestamp across all genuinely new copies in a chunk and the parent group's `updatedAt`, committed in one Firestore batch.
- Firestore Rules reuse `historyGrantBarrier(groupId)` for copy creation; standalone copy writes are denied.
- New copy creation is now basis-visible because group purge state already binds the group document update time. Together with 0.9.6.17 new-grant barrier, neither a new grant nor a new copy can appear after planning without invalidating the purge basis.
- Existing-copy retries remain idempotent: when no new copy is written, no group barrier touch is generated.
- Group source delete remains disabled pending one revalidated trace commit; do not bypass `GROUP_PURGE_TRACE_DELETE_NOT_READY` yet.
- Version advanced 0.9.6.17 -> 0.9.6.18. No live Firebase/htest/App Check/FCM change.
- Next: bounded atomic/revalidated receipt/grant/source deletion, followed by local cache/Outbox anti-resurrection and attachments.
- Overall first complete rebuild remains approximately 65%.

2026-09-06 — 0.9.6.19 GROUP RECEIPT PURGE BARRIER
- Clean gate `34055638377` passed all baseline steps, including the dedicated receipt barrier and group emulator coverage.
- New e2ee:4 group messages carry outer `receiptRevision: 0`.
- Existing `firebase.js` receipt transaction now reads parent message + own receipt together and advances parent `receiptRevision` exactly once for each real Delivered/Read mutation.
- Rules couple receipt create/update to the same atomic parent +1 revision and allow no other client mutation of the group message.
- Repeat receipt no-ops do not advance the barrier. First Read remains immutable/server-backed as before.
- Because group purge basis already includes the source message update time, concurrent receipt creation/advance is now basis-visible and cannot leave an orphan receipt behind a stale source purge.
- Group source delete remains disabled pending the server trace commit and local/offline anti-resurrection.
- Version 0.9.6.18 -> 0.9.6.19. No live Firebase, htest, App Check enforcement or FCM change.
- Overall first complete rebuild remains approximately 65%.

2026-09-06 — 0.9.6.20 ATOMIC GROUP PHYSICAL TRACE COMMIT
- `disappearing-purge-firestore-admin-adapter.mjs` now owns the complete server-only group Firestore trace commit.
- The commit transaction re-reads group/source/epoch/receipts/all grants/all grant copies, verifies the exact opaque basis, recomputes the pure grant trace plan, then performs all writes atomically.
- It deletes all source receipts, source-linked grant copies, empty grants and the source; retained grants are reconciled to the remaining count and earliest retained source.
- Any stale receipt/grant/copy/group/source/epoch basis fails closed before mutation. Already-absent source is idempotent success.
- Browser delete Rules remain closed; no tombstone, scheduler, live Firebase or htest change.
- Local/offline anti-resurrection, attachment traces and user-facing disappearing controls remain unfinished.
- Version 0.9.6.19 -> 0.9.6.20. Overall first rebuild remains approximately 65%.

2026-09-06 — 0.9.6.21 LOCAL ANTI-RESURRECTION DECISION FOUNDATION
- Added pure `disappearing-local-convergence.js`.
- Local purge planning requires valid disappearing metadata + explicit prior server-backed observation + absence from a server-backed authoritative snapshot.
- Cache-only absence and client/device clocks are never purge authority. Never-server-backed queued work remains valid pending work.
- Planner identifies matching encrypted Outbox IDs for physical removal but does not itself own IndexedDB or mutate app state.
- No local/cloud tombstones are created.
- Next slice must wire this decision through the existing UID-scoped IndexedDB/application owner, preserve metadata on direct/group projections, remove matching cache/Outbox traces before retry, and prove restart/reconnect non-resurrection.
- Version 0.9.6.20 -> 0.9.6.21. No live Firebase/htest change. Overall first rebuild remains approximately 65%.

FIDUNIO 1.0 PRE-ALLOCATED BUILD-NUMBER RULE — USER REQUIREMENT
- `FIDUNIO-BUILD-CHECKLIST.md` must carry the detailed implementation task and an already-allocated target build number for every remaining component required for 1.0.0, including work that is only planned.
- Current allocation bands: 0.9.6.x disappearing/anti-resurrection; 0.9.7.x attachments; 0.9.8.x invitations/install; 0.9.9.x UI/regression/release-candidate; 1.0.0 final promotion.
- A reserved number is not the runtime version until that slice is materially implemented. If an unexpected security/regression build must be inserted, update the checklist allocation before coding and never silently reuse a completed number.
- Every substantive build report must identify its allocated build number, detailed steps completed/remaining, validation evidence, and overall first-rebuild completion percentage.



WEIGHTED 1.0 COMPLETION LEDGER — USER REQUIREMENT
- FIDUNIO 1.0 completion is now a fixed 100.0-point weighted ledger in `FIDUNIO-BUILD-CHECKLIST.md`, not a subjective estimate or raw build count.
- Audited/repository-validated foundation through 0.9.6.20 is frozen at 66.0 earned points. 0.9.6.21 earns 1.0, making the current authoritative completion 67.0%.
- Every remaining allocated 1.0 build has a fixed point value. Reserved/current/incomplete builds earn zero until exit criteria + required repository gate are green; real-device points remain unearned until device proof.
- Regression removes points. Newly discovered mandatory 1.0 work requires explicit point reallocation before implementation while denominator remains 100.0.
- FCM 1.1 and App Check enforcement 1.2 remain outside the 1.0 denominator.

2026-09-06 — 0.9.6.22 UID-SCOPED LOCAL PHYSICAL PURGE WIRING
- Existing `app.js` remains the local state/history/Outbox mutation owner; no second IndexedDB owner was introduced.
- Added serialized `purgeLocalDisappearingMessageTraces(uid,messageIds)` with active Firebase UID guard.
- Planned IDs are physically omitted from in-memory messages and decrypted/re-encrypted history, matching encrypted Outbox rows are deleted, and cleaned app state is persisted.
- Restored queued messages retain `disappearAfterSeconds` and are explicitly never-server-backed (`serverBacked:false`).
- Added pure `disappearing-local-storage-plan.js` + focused test and normal gate step.
- 0.9.6.23 must supply authoritative direct/group server-backed observation/absence; this build does not authorize cache-only purge.
- Version advanced 0.9.6.21 -> 0.9.6.22. Full gate pending at documentation time; Full gate `34058248816` is SUCCESS; 0.9.6.22 earns its allocated +1.0 point and authoritative completion is now 68%. No live Firebase/htest change.

FIDUNIO 0.9.6.23 AUTHORITATIVE PROJECTION CONVERGENCE (2026-09-06)
- Runtime version: 0.9.6.23.
- Full baseline security gate 34059145963: SUCCESS.
- New pure owner `disappearing-authoritative-projection.js`: cache-only snapshots can merge but never purge; server snapshots mark observed rows serverBacked and plan exact disappearing absence convergence.
- app.js direct/group projection paths call the existing serialized active-UID local physical purge owner before authoritative projection persistence. Matching encrypted Outbox/history/live state traces are physically removed; no tombstone is created.
- Group conversation owner propagates Firestore snapshot metadata and preserves `disappearAfterSeconds`. Group history-grant authority reads are server-only when recomputing projection, preventing stale cached grant copies from being accepted as authoritative after source purge; offline local history remains cache-only and non-purge-authoritative.
- Live Firebase and htest were NOT touched. FCM remains 1.1; App Check enforcement remains OFF/deferred to 1.2.
- Weighted 1.0 ledger: 69.0/100.0 = 69%.
- NEXT: 0.9.6.24 restart/reconnect stale-client anti-resurrection proof. Must prove convergence occurs before Outbox retry after cold start/reconnect and must not use client time, tombstones, reload, or retry timers as semantic authority.

### 0.9.6.23 final grant-source authority strengthening
Final review caught and closed a subtle group-history resurrection boundary: a grant-only projected copy is not proof that its original source message still exists. `e2ee-account-group-history-projection.js` now marks ordinary retained source rows `authoritativeSource:true` and grant-only rows `authoritativeSource:false`; `disappearing-authoritative-projection.js` excludes grant-only rows from authoritative source-presence IDs and suppresses a grant-only row when authoritative source absence plans that disappearing ID for purge. The permanent projection and group-conversation gates cover this distinction. Full baseline security gate `34059145963` passed on the strengthened implementation. Live Firebase and `htest` were not touched.

### Mandatory build reallocation after 0.9.6.23
Repository review reconfirmed the known 0.9.6.20 omission: group physical purge still lacks an explicit conservative transaction write-count ceiling. Per the pre-allocation rule, this security repair is allocated **before implementation** as 0.9.6.24. The previous 0.9.6.24–0.9.6.29 work shifts to 0.9.6.25–0.9.6.30. The weighted denominator remains 100: 0.9.6.24 receives 0.5 point and restart/reconnect 0.9.6.25 receives 0.5 point; later shifted builds retain their prior points. Current earned completion remains 69%.

### 0.9.6.24 bounded group-purge transaction writes
The known 0.9.6.20 write-limit omission is closed. `disappearing-purge-firestore-admin-adapter.mjs` now computes the complete group purge transaction write count before scheduling any mutation and caps it at 400. Exactly 400 writes are accepted; 401 fails closed with `PURGE_TRACE_TOO_LARGE` and zero scheduled writes. Oversized traces remain intact for a future safe lease/chunk design; there is no source-first chunking, tombstone, browser delete permission, or live Firebase change. Full baseline security gate `34059468538` passed. Runtime version is 0.9.6.24. Weighted ledger is 69.5/100, reported as **70%**. Next allocated build is 0.9.6.25 restart/reconnect stale-client anti-resurrection proof.

### 0.9.6.25 restart/reconnect replay barrier — IN PROGRESS
Runtime version is now **0.9.6.25**. `disappearing-reconnect-recovery.js` is the pure restart/reconnect Outbox decision owner. `firebase.js` remains the sole client Firebase owner and now exposes explicit server-only direct/group source-ID probes; `app.js` remains the sole local mutation/controller owner and serializes reconnect reconciliation before any cloud/group Outbox replay. Server-present Outbox IDs are treated as already accepted, prior-server-backed disappearing IDs that are authoritatively absent are physically purged before retry, and only never-server-backed absent work may replay. Cache-only absence and client time remain non-authoritative; no tombstone is created. Full baseline security gate **34060082292** passed on head `cb05fadc749fd42cdbbccc64fc3bb1972b854d41`.

The build is deliberately **not** marked repository-validated for the 1.0 ledger because one exact crash/idempotency boundary remains unresolved: if Firestore accepts a send and the browser crashes before the Outbox is deleted or any durable `serverBacked:true` observation is recorded, then the source later expires and is physically purged before that device returns, restart state is indistinguishable from genuinely never-sent queued work. A per-message tombstone/accepted-ID registry would violate the current trace-free rule; delaying purge until sender acknowledgement can violate disappearance semantics; a durable per-device/replay-channel high-water mark may solve replay but would intentionally retain non-content anti-replay state caused by expired traffic and therefore needs an explicit architecture/privacy decision before adoption. The contract records `postCommitPreObservationCrashGapClosed:false`. No live Firebase or `htest` change was made. The allocated 0.5 point remains unearned, so weighted completion stays **69.5/100 (70% normal status)**. Do not begin 0.9.6.26 until this boundary is resolved.

### 0.9.6.25 FAIL-CLOSED REPLAY DECISION — USER APPROVED
User explicitly approved fail-closed handling for the rare Firestore-accepted/browser-crash-before-observation ambiguity. Persist a local Outbox `sendAttempted` marker before the cloud write. After restart, authoritative absence of an attempted row must never auto-replay; show/retain it as failed so the sender may deliberately create a new message. Never solve this with a per-message server tombstone or accepted-ID registry. Never let client time authorize the decision. Full gate `34060880885` passed. 0.9.6.25 is REPOSITORY-VALIDATED; weighted completion 70.0/100; next build 0.9.6.26.

### 0.9.6.26 + 0.9.6.27 COMBINED PASS — REPOSITORY-VALIDATED
User authorized building the two separately allocated checkpoints in one pass. 0.9.6.26 permanently gates same-UID independent-device expiry convergence and anti-replay. 0.9.6.27 exposes a compact composer duration selector (Off/5m/1h/1d/7d) and snapshots the normalized choice into each new direct/group message's immutable outer `disappearAfterSeconds`; preference changes affect only future messages. Full gate `34061730677` passed. Runtime 0.9.6.27; weighted completion 72%; next 0.9.6.28. No live Firebase/htest.

## 0.9.6.28-0.9.6.30 combined validated pass — 2026-09-06
Runtime is 0.9.6.30. Builds 0.9.6.28, 0.9.6.29 and 0.9.6.30 are separately REPOSITORY-VALIDATED by full Rebuild Baseline Security Gate 34062508968 SUCCESS. 0.9.6.28 closes the repository disappearing-text matrix; 0.9.6.29 wires admin-only beginning/date earlier-history intent through the existing server-backed grant runtime; 0.9.6.30 removes the duplicate direct Read mutation path and gates foreground/two-pane receipt lifecycle anchors. Weighted first-rebuild completion is 75.0/100.0 = 75%. Next allocated build is 0.9.7.0 attachment transport/data authority. Real-device receipt proof remains later acceptance work. Live Firebase and htest were not touched.

## 0.9.7.0–0.9.7.4 attachment send checkpoint — repository validated

Builds 0.9.7.0 through 0.9.7.4 establish one attachment send owner and wire photo, file, audio and video selection/capture through bounded local AES-256-GCM chunk encryption, encrypted Outbox staging, ciphertext-only Firebase Storage upload through `firebase.js`, and the existing direct/group E2EE message commit path. Attachment keys travel only inside E2EE message ciphertext. Size limits are photo 12 MiB, file 20 MiB, audio 25 MiB, video 50 MiB. Storage client delete is denied; disappearing attachment deletion remains reserved for the purge owner in 0.9.7.8. Full Rebuild Baseline Security Gate `34063327957` SUCCESS. No live Firebase or htest deployment occurred. Runtime version is 0.9.7.4. Next build is 0.9.7.5 receive/decrypt/display/play.

## 0.9.7.5–0.9.7.9 attachment phase closeout — repository validated

Runtime 0.9.7.9 completes the allocated attachment phase: integrity-checked receive/decrypt with explicit object-URL lifecycle; UID-scoped offline/cache recovery policy; message-level receipt authority; trace-free disappearing-attachment purge planning and serialized storage/local-before-source execution; and the permanent attachment closeout matrix. Firebase Storage download remains solely in `firebase.js`. Client Storage deletion remains denied; server purge dependencies are injected into the dedicated purge executor and no live Firebase deployment occurred. Full Rebuild Baseline Security Gate `34064314857` SUCCESS. Next allocated build is 0.9.8.0 invitation deterministic-owner rebuild; rejected 0.9.4.12-.15 invite/install logic remains forbidden.

## 0.9.8.0–0.9.8.5 invitation/install checkpoint — repository validated

Runtime 0.9.8.5 completes the invitation/join/install phase. `invitation-owner.js` is the sole serialized invitation mutation coordinator while `firebase.js` remains the sole Firebase repository/SDK owner. Pure `invitation-policy.js` enforces single-use lifecycle, issuer roles and target roles. Auth and Settings request invitation work through that owner. Firestore emulator coverage proves anonymous validation of a known token, unauthorized issuance/revocation denial, owner issuance/revocation, atomic accepted-invitation + active-profile enrollment, and second-redemption denial. Joined active profiles flow into existing direct/group discovery without device binding.

`install-guidance.js` owns only an optional predefined Settings Install panel. It never mutates invitation, account, messaging or service-worker state and never uses automatic install prompting. iOS uses Safari Share -> Add to Home Screen; Android/Fire and desktop use browser-provided install/add/shortcut commands when available. The rejected 0.9.4.12–0.9.4.15 invite/install logic was not restored or adapted. Protected iPhone Back/wrap, Settings deterministic ownership and two-pane architecture remain gated. Full Rebuild Baseline Security Gate `34065528714` SUCCESS. No live Firebase or htest deployment occurred. Next allocated build: 0.9.9.0 Group Info completion.


## 0.9.9.0-0.9.9.5 repository candidate checkpoint — 2026-09-06
Group Info landscape/responsive completion, real Direct Chat Info-only behavior, prototype/simulation retirement and responsive/lifecycle hardening are repository-validated. Permanent candidate UI/lifecycle coverage is in the normal baseline. Full gate 34066875377 SUCCESS. Runtime checkpoint 0.9.9.5. Weighted ledger 95.5/100 (reported 96%). Next: 0.9.9.6 atomic htest deployment. Do not claim 0.9.9.7 device acceptance until the user actually tests iPhone/iPad/two-account/offline/disappearing/attachments/invitation/install/recovery. Live Firebase is still not to be changed.


## 0.9.9.6 htest deployment checkpoint — 2026-09-06
Full gate 34067021824 SUCCESS on exact candidate 31dac2c0b551429a5a4ba7cf7c967e6316e9a9e6; htest branch created at that exact SHA with runtime 0.9.9.6. Weighted ledger 96.0/100. 0.9.9.7 is now CURRENT but BLOCKED — USER DEVICE PROOF. Do not award 0.9.9.7, run 0.9.9.8 stabilization, or claim 0.9.9.9 promotion readiness without actual device acceptance evidence. Live Firebase remains untouched.


## 0.9.9.6 htest deployment checkpoint — 2026-09-06
Full gate 34067021824 SUCCESS on exact candidate 31dac2c0b551429a5a4ba7cf7c967e6316e9a9e6; htest branch created at that exact SHA with runtime 0.9.9.6. Weighted ledger 96.0/100. 0.9.9.7 is now CURRENT but BLOCKED — USER DEVICE PROOF. Do not award 0.9.9.7, run 0.9.9.8 stabilization, or claim 0.9.9.9 promotion readiness without actual device acceptance evidence. Live Firebase remains untouched.


## 0.9.9.8 Firebase Storage connectivity repair — IN PROGRESS — 2026-09-06
The 0.9.9.7 pre-acceptance connectivity check proved that the prior 0.9.7.x attachment gates were repository-only dependency-injection/source tests, not a real Firebase-backed attachment test. The live default bucket `fidunio-fef13.firebasestorage.app` was then created in `US-CENTRAL1`, the reviewed `storage.rules` compiled and deployed, and Firebase granted the required Storage-Rules-to-Firestore cross-service role. Firestore rules, Functions, Hosting, Auth, App Check, GitHub branches and protected Firebase configuration were not changed by that live setup.

Repository stabilization now registers `storage.rules` in `firebase.json` and adds a permanent Storage deployment-wiring gate. Runtime advances 0.9.9.6 -> 0.9.9.8 because 0.9.9.7 is the device-acceptance gate, not an implementation build. This does not complete attachment acceptance: authenticated real-device upload/download/authorization/offline/purge proof remains required. The 0.9.9.8 point remains unearned until the full repository gate is green and acceptance defects are closed. Overall ledger remains 96.0/100 (96%).


### 0.9.9.8 repository validation checkpoint — 2026-09-06
Commit `34c8d237eb8f08b8228f670b8ca958038b553aef` registers `storage.rules` in `firebase.json`, adds the permanent `storage-deployment-wiring.test.mjs` gate, advances runtime to 0.9.9.8, and preserves `firebase-config.js` unchanged at blob `b81026dcc07b7374d1f48d0cb094764ce28319bd`. Full Rebuild Baseline Security Gate `34072294756` completed SUCCESS, including the new Firebase Storage deployment-wiring step. This proves repository deployability, not real attachment operation. 0.9.9.8 remains IN PROGRESS pending authenticated iPhone/iPad upload, second-device download/decrypt, unauthorized denial, offline/reconnect and disappearing-attachment purge proof. No additional completion credit is earned; overall remains 96.0/100 (96%).


## 0.9.9.7 iPad acceptance failure — 2026-09-06

Real-device testing on the promoted `main` build exposed three release-candidate blockers: missing conversation widgets, materially regressed iPad text size, and conversation-pane overlap. The durable detailed records are FDA-IPAD-001 through FDA-IPAD-003 in `DEVICE-ACCEPTANCE-BUGS.md`. These are user-observed device failures, not repository simulations. Build 0.9.9.7 remains FAILED/BLOCKED; 0.9.9.8 remains the allocated RC stabilization build and earns no point until repair, full required gates, redeployment and repeated user acceptance. Overall completion remains 96.0/100.0 (96%).


## 0.9.9.8 iPad RC repair candidate — 2026-09-07

The bounded repair candidate addresses FDA-IPAD-001 through FDA-IPAD-003 without changing Firebase/E2EE/storage ownership. It separates Sign Out from the constrained tablet icon cluster, makes tablet brand/navigation/tool labels scale from the established root A/A+/A++ owner, changes the obsolete eight-slot tablet attachment grid to the four supported tools, and distinguishes Firebase conversation discovery from authoritative empty/error state. It does not fabricate conversations or restore quarantined cross-account data. Targeted iPad stabilization, release-candidate UI/lifecycle and Storage-wiring gates pass locally. Full baseline, promotion to `main`, Pages deployment and repeated user-device acceptance remain pending. Defects remain OPEN; 0.9.9.8 earns no point and total completion remains 96.0/100.0 (96%).


## 0.9.9.8 second iPad acceptance evidence — 2026-09-07

The first repair restored the real cloud conversation and composer/widgets and removed the horizontal Sign Out collision. The user screenshot also exposed FDA-IPAD-004 (standalone status-bar header clipping) and FDA-IPAD-005 (unused right-side viewport). A tablet-only follow-up candidate adds a bounded safe-top fallback and gives the existing `#app`/tablet-shell owner full flex width. Targeted iPad, candidate UI and receipt-lifecycle gates pass locally. All five device defects remain open until full baseline, `main` deployment and repeated user acceptance. Completion remains 96.0/100.0 (96%).


## 0.9.9.8 third iPad acceptance finding — 2026-09-07

The second screenshot confirms the safe-area and full-width repairs visually, while exposing FDA-IPAD-006: iPad painted the native disappearing selector white inside the dark composer. The bounded CSS-only candidate themes the existing selector with the established panel/accent/ink variables and preserves an accessible 42px target. No render, Firebase, storage, E2EE or receipt owner changes. Targeted gates and user acceptance remain required; completion remains 96%.

## FDA-IPAD-006 deferred visual-alignment decision — 2026-09-07

The deployed selector theme removed the white native appearance and remains readable, but the user reports that its size, shape, spacing and alignment do not aesthetically match the quick-reply widgets. By explicit user direction, do not make another isolated cosmetic change now. Keep FDA-IPAD-006 OPEN — DEFERRED and include its visual-alignment repair with the next necessary 0.9.9.8 acceptance defect repair. Preserve the existing disappearing-message owner and behavior, accessibility sizing, Firebase/E2EE/storage/receipt ownership and the protected Firebase configuration. Completion remains 96%.

## FDA-DM-001 direct-send stall diagnosis — 2026-09-07

The first resumed direct-message device test failed: `IPAD TEST 1` on iPad remained at Sending for more than one minute and did not reach the iPhone during observation. `sendCurrent()` stages the encrypted Outbox row and paints Sending before `flushQueuedAfterAuthoritativeReconcile()` completes. That path awaits authoritative Firebase server reads without a bounded timeout; therefore Safari can report online while a stalled server promise leaves the message indefinitely at Sending before the actual encrypted Firestore send. Do not infer that Firestore accepted the message. FDA-DM-001 is a critical OPEN 0.9.9.8 defect. Preserve the one serialized `app.js` Outbox/reconnect owner, sole `firebase.js` SDK owner, fail-closed attempted-send rule, protected Firebase configuration and real receipt authority. Never manufacture Sent/Delivered/Read. Completion remains 96%.

## FDA-DM-001 bounded repair candidate — 2026-09-07

The repair remains inside the existing `app.js` serialized Outbox/reconnect owner. New pure `outbox-reconciliation-boundary.js` bounds authoritative Firebase reconciliation at 12 seconds and selects only unattempted Outbox-backed Sending rows for requeue. On timeout, `app.js` returns those rows to Queued, preserves encrypted Outbox authority, persists/renders through its existing owner and explicitly reports that Firebase did not respond. Attempted rows are excluded by policy and remain governed by the 0.9.6.25 fail-closed replay rule. No Firebase SDK owner, rules, protected configuration, E2EE, receipt or service-worker semantics changed. Permanent gate plus targeted Outbox/receipt/runtime tests pass locally. Full baseline, `main` promotion and real iPad-to-iPhone acceptance remain required; completion remains 96%.

## FDA-DM-001 first repair rejected by device evidence; expanded candidate — 2026-09-07

After the first candidate was gated and promoted, `IPAD TEST 1` appeared Failed on entry. Sending two new messages caused the original row to show Sending again and both new rows remained Sending beyond one minute. The first reconciliation-only boundary is incomplete and earns no acceptance credit. Code diagnosis found the complete cycle was not serialized: lifecycle triggers serialized reconciliation but could overlap later Outbox flushes, while peer/key/envelope/Firestore confirmation waits were unbounded. Expanded candidate adds one `app.js` full-cycle tail, retains stage-specific 12-second boundaries, preserves Queued only before `sendAttempted`, preserves Failed after ambiguous attempt, and stops disallowed attempted rows from reverting to Queued. `service-worker.js` adds the pure boundary module to the shell and changes the non-product cache revision to `0.9.9.8-fda-dm-001b`; runtime version remains 0.9.9.8. No Firebase config/rules/SDK owner, E2EE owner, receipt authority or service-worker semantics changed. Targeted tests pass; full baseline, promotion and device proof remain required. Completion remains 96%.

### FDA-DM-001 expanded candidate repository/deployment checkpoint — 2026-09-07

Expanded candidate commit `90460aea19c0d5204c91b2542d59905b1edff246` passed full Rebuild Baseline Security Gate `34085040014`. It was promoted to `main` as `321d182cbd08cb690fa4df7caf96221ef69d09b4`; GitHub Pages run `34085354728` succeeded. Live `app.js` full-cycle serialization/stage boundaries, `outbox-reconciliation-boundary.js`, and service-worker revision `0.9.9.8-fda-dm-001b` were verified. Protected `firebase-config.js` remained blob `b81026dcc07b7374d1f48d0cb094764ce28319bd`; no Firebase rules/config were changed. FDA-DM-001 and 0.9.9.8 remain open pending fresh installed-iPad status behavior and authenticated Firebase Sent → Delivered → Read proof. Completion remains 96%.

### FDA-DM-001 bounded device result and FDA-IOS-001 deferred startup issue — 2026-09-07

After the required second installed-iPad launch, `IPAD TEST 1` and `ipad test 2` both display Queued. This is real-device evidence that the revised shell loaded, the bounded pre-attempt transition works, encrypted Outbox rows survive, and no Sent/Delivered/Read state is fabricated. It simultaneously proves authenticated Firebase communication did not complete during the boundary; pause further sends and diagnose that connectivity next. FDA-DM-001 remains open. The user separately reports that iPhone login/startup paused on a blank screen before the first screen appeared. Record this as minor FDA-IOS-001, OPEN — DEFERRED. Future repair should use the existing deterministic startup/auth owner to show a large accessible spinner/loading message; no second auth owner, polling, reload or timing lifecycle hack. Completion remains 96%.

### FDA-DM-001 authenticated-session/verification-path diagnosis — 2026-09-07

The user clarified that Verify was pressed independently on both devices without comparing fingerprints. Treat this only as a stored trust decision; do not infer matching keys and do not reset/regenerate keys. Code diagnosis found that the Verify handler directly invoked `flushQueued()`, bypassing the sole serialized authoritative reconciliation/Outbox cycle. The 0.9.9.8 candidate routes that trigger through `flushQueuedAfterAuthoritativeReconcile()`, force-refreshes the Firebase Auth ID token through sole SDK owner `firebase.js`, and adds bounded stage-specific diagnostics. Preserve Queued before attempt, Failed after ambiguous attempt, and real receipt authority. Cache revision becomes `0.9.9.8-fda-dm-001c`. Firebase rules and protected configuration remain unchanged. Completion remains 96% pending full gate, deployment and device proof.

Repository/deployment checkpoint: authoritative candidate `6e6d5e84beb7e12173a5708835842512d44a92d4`; full baseline `34087534090` SUCCESS; `main` promotion `529a56d1f8e45d463a47d8c6150f95b4937ee87b`; Firebase adapter `34087731108` SUCCESS; Pages `34087730948` SUCCESS. Live Auth-refresh, serialized Verify, stage-diagnostic and cache-revision anchors passed. Protected Firebase config remains exact blob `b81026dcc07b7374d1f48d0cb094764ce28319bd`; `config-firestore.js` remains absent. FDA-DM-001 and 0.9.9.8 remain open pending real device proof; completion remains 96%.

### FDA-DM-001 retry backlog plus FDA-IOS-001/002 startup repair — 2026-09-07

User device evidence: second open blanked for more than two minutes; previously configured iPhone PIN appeared unset; new `ipad test 3` stayed Sending and became Queued after close/reopen. This rejects the preceding device candidate while confirming Outbox preservation and no false receipt. Diagnosis: three lifecycle recovery triggers could accumulate behind the serialized 12-second cycle, while a forced network token refresh delayed every cycle. Separately, a local PIN IndexedDB read failure fell through to the default no-PIN state. Current 0.9.9.8 candidate coalesces triggers, uses a valid cached Firebase token with normal SDK refresh, retains visible stage errors, adds an immediate large startup spinner, and fails closed when PIN storage is unavailable. Never reset the PIN or keys. Cache revision is `0.9.9.8-fda-dm-001d`. Targeted permanent gates pass locally; full baseline/deployment/device proof remain required. Completion remains 96%.

Repository/deployment checkpoint: authoritative `429855f037c3f9f0fb2a0f31ff1371a21f273922`; complete baseline `34125430406` SUCCESS; promoted `main` `2dcbd8a558ed8bf355ebec1c6bbc82e59948b29f`; Pages `34125666157`, Firebase adapter `34125667262` and recovery security `34125666918` SUCCESS. Live anchors, cache `001d`, and exact protected Firebase config blob verified. FDA-DM-001, FDA-IOS-001 and FDA-IOS-002 remain open for device evidence. Completion remains 96%.

### Device evidence isolates Account E2EE lock — 2026-09-07

After two iPhone restarts, the large spinner stayed visible until the normal PIN screen; the original local PIN worked and Messages opened. iPad showed the same startup behavior on first restart. FDA-IOS-001 and FDA-IOS-002 are resolved/user-accepted. The iPad then showed `Firebase connection problem: Account E2EE identity is not unlocked for this account.` Interpret the underlying error correctly: the account-authoritative encryption service is fail-closed before encryption/send; this is not proof of Firebase unavailability. Queued Outbox rows remain safe. A changed peer fingerprint warning is also present. The user previously verified without comparison, so do not verify again until both devices' fingerprints are deliberately compared. Do not reset or regenerate identities. Next step is inspect Settings → Account Encryption and unlock the existing durable identity with the account password and exact six-digit E2EE PIN. Completion remains 96%.

### 0.9.9.8 outgoing v3 read diagnosis — 2026-09-07

Both real devices reached Account Encryption READY. The iPad's preserved messages then reached real Sent but displayed account encryption unavailable locally. Root cause: every v3 row used an incoming-only decrypt direction, so the sender could not read its own ciphertext. Repair remains within existing account crypto/service/runtime owners: direction comes from authenticated UID versus authoritative `senderUid`; actual sender/recipient AAD and keyIds remain bound; unknown senders fail closed. Permanent service tests cover incoming and own-outgoing reads. Changed-key UI is labeled legacy compatibility-device trust and is not accepted as account verification. Cache revision `001e`. Await full baseline, main promotion and real receipts. Completion remains 96%.

Initial full runs 34130208522 and 34130353052 failed at the Outbox gate because the permanent exact cache-revision assertion still expected 001d. All preceding gates passed. Correct the assertion to required 001e and rerun the complete baseline; do not weaken any security behavior.

Corrected authoritative commit fbfb296b2c29ce367fae7c38abc2b5147f14d509 passed complete baseline 34130779064 SUCCESS. This validates the sender-side v3 reader and all established gates. Still required: exact-tree main deployment and real iPad/iPhone readable Sent -> Delivered -> Read. No point awarded; completion remains 96%.

Final documented authoritative commit 243e1698f5efad09e03587cce0c8689c81ff4659 passed full baseline 34131152754 and its exact tree was promoted to main as a08d985a8308b9f9da9fabc3c127beefc452fe04. Pages 34131445802 succeeded. Protected Firebase config stayed exact and config-firestore.js stayed absent. Next: two-launch cache activation followed by readable iPad -> iPhone Sent -> Delivered -> Read. Completion remains 96%.

### MAIN-BRANCH SOLE AUTHORITY — 2026-09-07

The user explicitly ended the two-branch development/mirror process because editing, testing, promotion and document synchronization consumed unnecessary resources. From this point, `main` is the sole authority for application code, cumulative durable documents, full baseline testing and GitHub Pages device testing. `fidunio-complete-rebuild` is frozen as a historical checkpoint and must not be used for new changes. Remove `.github/workflows/mirror-rebuild-docs-to-main.yml`; retarget Rebuild Baseline Security Gate push events to `main`. Do not recreate branch mirroring without explicit new approval. Protected Firebase files remain intact. This changes process only and earns no product credit; completion remains 96%.

### 0.9.9.8 READONLY OUTGOING ROW — 2026-09-07

Real iPad retest showed `Attempted to assign to readonly property` and test msg 4 stuck Sending. This is not a Firebase outage. `stampOutgoingDisappearSelection` froze the full row, but sole `app.js` Outbox owner must mutate transport state. Repair returns a normal application-owned row after stamping expiry once; regression test proves Sending → Sent is allowed without changing expiry. No new owner. Cache revision `001f`. Await full main gate, Pages and device receipts. Completion remains 96%.

### FDA-DM-002 PENDING-MESSAGE DELETION — 2026-09-07

The user identified missing deletion as a release blocker because garbage Queued/Sending/Failed rows could not be removed. The 0.9.9.8 candidate adds press-and-hold Delete Message/Cancel UI only for pending outgoing rows. `app.js` remains sole encrypted Outbox owner: cancellation reserves against active work, rechecks the row and uses the established physical local purge owner. No Firestore delete, tombstone, duplicate retry or receipt path is added. Cache revision `001g`. Full gate, Pages and device restart/reconnect proof remain required. Completion remains 96%.

### SENT DIRECT DELETION AND ONE-PIN UX DECISION — 2026-09-07

The user confirmed pending deletion works and required deletion of already-accepted encrypted garbage messages. A repository candidate adds server-only sender/member-authorized direct-message physical deletion, deleting deterministic attachment traces before the message source. Client Firestore delete remains denied. It requires a dedicated message-delete service account and explicit Function deployment; it is not live yet. `MESSAGE-DELETION-AUTHORITY.md` is now critical.

The user rejected multiple visible key/PIN concepts. Approved fresh-account UX: email/password plus one six-digit FIDUNIO PIN, optional biometrics, and all device/account/direct/group/attachment/fingerprint mechanics in the background. Local-unlock and account-encryption derivations remain internally separate and domain-separated. `USER-ACCESS-KEY-UX.md` is critical. Implementation and clean-device acceptance remain incomplete. Completion remains 96%.

The 0.9.9.8 single-PIN candidate implements one Security area and removes ordinary Device Identity, separate Account Encryption, technical fingerprint/key, independent local-PIN controls and Firebase-specific account wording. New PINs are six digits. First setup writes the separate local verifier after E2EE succeeds; an existing different verifier fails closed and is never overwritten. Legacy 4–12 digit verification remains only for migration compatibility. Permanent UX gate added; full baseline/deployment/device proof pending. Completion remains 96%.

Single-PIN/delete candidate `5e22a9484029d02f9f6691b82329a55d4695c848` passed complete baseline `34139245770`; Pages `34139244639` succeeded. Delete for Everyone remains client-disabled pending explicit live provisioning/deployment of its dedicated callable. User-device acceptance remains unearned. Completion remains 96%.
0.9.9.9o fixes attachment receive presentation. The encrypted attachment descriptor remains the message transport envelope, but it is no longer shown to the user. app.js parses it, calls the existing attachment receive service, downloads and integrity-checks encrypted chunks, decrypts locally, and renders photo/file UI with loading and retry states. Conversation previews use a friendly attachment label. Temporary object URLs are released on sign-out.
0.9.9.9p fixes attachment downloads remaining in Loading on both sender and receiver. Firebase encrypted chunks are fetched using a bounded six-worker pool rather than serially, their original order is preserved, each read has a 30-second timeout feeding the retry UI, and firebase-storage.js is included in the installed service-worker shell.
0.9.9.9q gives the attachment sender an immediate zero-copy preview. On file selection the app creates an object URL pointing at the browser File and renders the optimistic Sending message before reading, encrypting, or uploading bytes. The URL is retained for the session and revoked at sign-out; recipients and restarted sessions use the encrypted Storage download path.
0.9.9.9r compresses photos locally before encryption. The longest edge is bounded at 1600 pixels and the image is JPEG encoded at quality 0.76; the result is retained only if smaller. The compressed bytes are then encrypted and uploaded, so Storage remains ciphertext-only. Recipient decompression is the browser's normal JPEG decode after attachment decryption. Conversion failures fall back to the original file.
0.9.9.9s records the corrected attachment diagnosis: no remote Firebase-backed picture download had ever passed; the visible sender image was only the temporary object URL. The generic UI had swallowed the causal error. Upload now verifies metadata for the manifest and all chunks before message commit. Download errors retain a bounded Manifest/Chunk n stage and Firebase code, while verification/decryption failures are distinguished. Full paths and keys are not displayed.
0.9.9.9t responds to the first definitive device error: Manifest download failed with storage/timeout, while upload-side metadata verification proved the encrypted object existed. The Firebase SDK getBytes transport is replaced by authenticated getDownloadURL plus browser fetch with a 15-second abort, HTTP validation and a one-megabyte object cap. Download URLs remain ephemeral in memory and never enter messages or persisted state.

2026-09-08 — Issue 2 candidate 0.9.9.9y: device report showed a text sent just after Wi-Fi→LTE cleared the composer but did not appear in the conversation until much later, creating duplicate-send risk. Source review proved `sendCurrent()` staged the row but awaited encrypted IndexedDB Outbox creation and state persistence before rendering. Candidate renders the staged row first, preserves the serialized Outbox as durable authority, and marks that same row Failed if durable queueing fails. Permanent regression coverage added. Issue 3 remains untouched. Device LTE acceptance pending.

2026-09-08 — Issue 3 candidate 0.9.9.9z: iPhone chat bottom clearance now measures the real fixed composer height instead of assuming 170px; the phone bottom-scroll path runs only after that inset is applied and updates when the textarea grows. Wide/iPad scroll behavior remains unchanged. Video now offers Photo Library or Camera and both choices reuse the existing accepted video picker/send pipeline. Device acceptance pending.

2026-09-08 — Audio candidate 0.9.9.10: iPhone Audio previously used a generic file input with an audio capture hint, which could open video capture; the returned video MIME was then rejected by the existing audio validator. Audio now opens Record Audio / Choose Audio File / Cancel. Record Audio is microphone-only MediaRecorder capture; saved audio uses audio/* without capture. Both converge on the existing attachment send pipeline. Device acceptance pending.

2026-09-08 — Audio 0.9.9.11 candidate: 0.9.9.10 device test proved chooser and microphone recorder correct but recorded audio failed pre-send validation. Source boundary was MediaRecorder MIME passed verbatim; codec-parameterized audio MIME is now canonicalized to its base type before File creation/validation. No video MIME coercion, transport, encryption, Storage, Outbox, or size-limit changes. Device acceptance pending.

## FIDUNIO 1.1.6 notification routing checkpoint — 2026-09-09

- N4 real-device acceptance passed: a backgrounded iPhone received the generic `FIDUNIO — New message` notification after the live Eventarc/Cloud Run invocation permission was corrected.
- Live N4 trigger: `notifyDirectMessageCreatedV1`; Eventarc trigger region is `nam5`; the trigger service account has `roles/run.invoker` only on the N4 Cloud Run service.
- N5 candidate adds deterministic notification-tap routing. The service worker may focus/open FIDUNIO and pass only opaque direct-message routing intent. `app.js` remains the route/message owner and resolves the conversation through existing Firestore/E2EE state.
- Notification metadata never creates a message row, decrypts content, or writes receipts. Cold-open routing waits behind the normal local PIN/auth gates.
- Device acceptance still required for N5 warm-open and cold-open taps before N5 is closed.

## FIDUNIO 1.1.9 — optional sender display-name notifications — 2026-09-09

**Status: REPOSITORY CANDIDATE; live Firestore rules + N4 Function deployment and device acceptance required.** Private notifications remain the default. Each notification installation may explicitly opt in with `showSenderName: true`. The server resolves the sender only from authoritative `users/{senderUid}.displayName`, never from message `senderName`, and sends `FIDUNIO — New message from <display name>` only to opted-in installations. Non-opted installations remain `FIDUNIO — New message`. Message text, attachment names, email, UID, phone, ciphertext and decrypted content remain excluded. This changes no Firestore/E2EE/message/receipt authority.


FIDUNIO 1.1.9 DOCUMENTATION-ONLY AUDIT — 2026-09-10

The exact frontend checkpoint remains commit a3830afd8a01f88448e42703ac4b803058451ef8, visible version 1.1.9. The user confirmed that fcm.txt completed successfully and printed the backend/rules restoration success result.

Fresh device evidence after restoration: both iPhone and iPad now follow notification -> tap -> PIN -> Settings. The intended direct conversation is not selected. iPhone notification arrival was approximately 1–2 seconds. The reported iPad delivery event was approximately 30 seconds or more. This invalidates the assumption that the historical iPhone routing success remains reproducible today.

Read-only investigation confirms that app.js correctly retains and applies a notification route when one is actually supplied. Current evidence shows no route reaches app.js. The backend uses an FCM notification-plus-data message, while service-worker.js expects its own push/notificationclick handling to construct the routed URL. Do not change PIN, Settings or conversation selection to compensate for absent routing input.

ROGUE-CODE-RAMIFICATIONS.md is the durable authority for the complete rogue-code call graph, affected screens, affected functionality and mandatory retest matrix. It covers scheduleReconnectRecovery(), recoverForegroundCloudSession(), online/offline handlers, visibilitychange/pageshow, initializeFirebaseLayer(), applyPendingNotificationRoute(), firebase.js messageStreams ownership and delayed direct-message projection. Discovery is not proof of causation and no cleanup is approved.

Mandatory separation: notification routing redesign, lifecycle cleanup, and iPad/direct projection latency optimization must be different proposals and different releases. No code, version, service-worker revision, Firebase backend, Firestore rule, Pages deployment or device-acceptance status changed in this documentation checkpoint.


FIDUNIO 1.1.17 NOTIFICATION OWNER CANDIDATE — 2026-09-10

Implemented the approved bounded redesign without touching rogue lifecycle or projection latency. The backend common notification-plus-data payload is replaced by data-only FCM containing type, conversationId, messageId and bounded notificationBody. The installed PWA service worker is now a module and owns Firebase Messaging background receipt/display through onBackgroundMessage. It uses the unchanged firebase-config.js only to initialize a named Messaging-only worker app; no Auth/Firestore/App Check/Functions/Storage service is initialized in the worker. The existing notificationclick routed URL, PIN gate and app.js route application are preserved.

Version is 1.1.17; rejected 1.1.9.1–1.1.16 are not reused. No lifecycle cleanup, Outbox, receipt, E2EE, Settings, group, attachment or disappearing-content code changed. Repository gates, Pages, live Function deployment and real iPhone/iPad proof remain required before acceptance.


### 1.1.17 baseline rerun correction — 2026-09-10

Initial full gate run 34436219194 passed all notification stages and the first 73 security stages, then stopped because outbox-reconciliation-boundary.test.mjs still required the historical 1.1.9 service-worker revision. The assertion is updated to require the exact 1.1.17-data-only-sw-owner revision. No runtime, Outbox or notification behavior changed and no invariant was weakened. A complete rerun is required.


### 1.1.17 second baseline rerun correction — 2026-09-10

Full gate run 34436403470 passed stages 1–81, including all notification, lifecycle, Outbox, attachment, E2EE, PIN and deletion gates, then stopped at direct-message-basic-path.test.mjs because its installed-PWA assertion also retained the historical 1.1.9 shell revision. A scan of all 78 permanent test files confirmed this was the only remaining old-revision assertion. It is updated to require 1.1.17-data-only-sw-owner. No runtime behavior or invariant changed. A third complete run is required.

FIDUNIO 1.1.18 REJECTED; 1.1.17 FRONTEND RESTORED — 2026-09-10

The reviewed 1.1.17 Function deployed successfully. The 1.1.18 returned-client message experiment did not improve iPad notification routing: notification -> PIN -> Settings failed twice. The user reported that 1.1.17 was more stable and performed better on iPhone. All 1.1.18 runtime and test-expectation changes are removed; version.js and the deterministic service-worker cache revision return exactly to 1.1.17. The live 1.1.17 backend remains unchanged. Clean-baseline evidence also records that an iPad message may already be delivered but remain absent until leaving/re-entering the conversation, with another appearing after about five seconds; that projection/lifecycle behavior remains separate from notification-click routing under ROGUE-CODE-RAMIFICATIONS.md.

FIDUNIO 1.1.19 ONE-SHOT INTENSIVE DIAGNOSTIC — 2026-09-10

User authorized exactly one final diagnostic release and requested maximum useful data. Earlier diagnostic findings were recovered from the handoff before coding. A dedicated `fidunio-notification-diagnostics-v1` IndexedDB ledger now serializes cross-context events from Firebase Messaging background receipt through OS display/click, client enumeration/open result, bootstrap, PIN, hydration/Firebase readiness, route application and direct-message projection/render. The report retains full opaque account/conversation/message IDs and URLs. PIN/password/private/recovery key and auth/FCM token values remain prohibited because capturing them is unnecessary and would compromise the secure messenger; presence/length/suffix metadata is retained. Actual routing, Settings ownership, backend, rules/config, Outbox, receipts and E2EE are unchanged.

### 1.1.19 one-shot iPad result and binding diagnosis — 2026-09-10

The complete 60-entry iPad report is decisive. Firebase Messaging delivered the correct data-only direct-message envelope at `06:20:10.759`; the worker displayed it at `06:20:10.845`. The ledger contains no `notification-click`, `open-window-request` or `open-window-result`. iPadOS/WebKit subsequently resumed the installed PWA at plain `/hermes/`, with no notification query and `pendingNotificationRoute: null` before and after PIN, hydration and Firebase readiness. Therefore FIDUNIO's worker click handler did not receive the tap and `app.js` had no notification route to apply. This rules out Settings overwrite, PIN route loss, `applyPendingNotificationRoute`, backend payload construction, Firestore delivery and E2EE as the cause of FDA-NOTIFY-002.

After PIN the direct conversation opened only because persisted application state already held that same conversation in `selectedId`; it was not notification routing. The later Settings render was the user's navigation to retrieve diagnostics. The message was present in the authoritative 48-row Firestore snapshot. Snapshot receipt began at `06:21:07.300`, while projection/cache/persist reached completion at approximately `06:21:11.319`, exposing about 3.7–4.0 seconds of local projection work plus repeated roughly three-second subscription/projection passes. That is separate FDA-DM lifecycle/performance evidence, not notification-tap causation.

No further diagnostic release is authorized or needed. The next bounded repair is an installation-local pending-notification inbox written by the service worker before display and consumed after PIN. A sole pending unread conversation may open directly; multiple pending conversations require a chooser rather than a guess. Because iPad supplies no observable tap signal, a manual app launch after notification may also consume/open the pending conversation; this tradeoff must be preserved in implementation documentation and acceptance. Do not use a UID-global consumable route. Remove the temporary 1.1.19 diagnostic instrumentation as part of the repair build, but preserve this evidence permanently.

### FIDUNIO 1.1.20 installation-local notification inbox candidate — 2026-09-10

The bounded repair implements the 1.1.19 conclusion without changing Firebase backend/rules, PIN/auth, Firestore, E2EE, Outbox, receipts or rogue lifecycle owners. Before displaying a validated background direct-message notification, the service worker writes its opaque type/conversationId/messageId to `fidunio-notification-pending-v1`. After ordinary app hydration, PIN unlock and authenticated Firebase readiness, `app.js` reads that installation-local inbox exactly once. Messages for one conversation collapse to the newest route and are consumed together after successful selection. Multiple conversations show a neutral New messages chooser; no UID-global route and no timer/guess is used. The click URL remains as an iPhone-compatible fast path and consumes the matching inbox record.

The one-shot diagnostic page, ledger, instrumentation and baseline gate are removed; the diagnostic conclusions remain in durable documents. Explicit tradeoff: because the affected iPad exposes no click event, manually opening FIDUNIO after a displayed notification can also open the sole pending conversation. Touched screens are startup/PIN transition, direct conversation, and the new multiple-conversation chooser; Settings only loses the temporary diagnostics link. Required device proof covers iPhone/iPad, notification -> PIN -> exact direct conversation from Settings and other prior screens, ordinary no-notification resume, one versus multiple pending conversations, message presence, receipts, attachment continuity, and two-launch service-worker activation. Version/cache are 1.1.20 / `1.1.20-installation-notification-inbox`.

### FIDUNIO 1.1.21 single activation and composer projection owner — 2026-09-10

User-device evidence rejected 1.1.20: the iPad routed twice then stopped and iPhone did not route. The exact defect was a process-lifetime `notificationInboxLoaded` cache. After any first read, later service-worker writes were ignored by the still-running PWA. Version 1.1.21 removes that cache. One `requestAppActivation()` promise owner now coalesces initial hydration, unlock, auth readiness, foreground/pageshow/visibility, connectivity and service-worker route signals; an explicit reason queue retains signals raised during awaited work until the same owner drains them. No busy/follow-up flag is used. Those handlers no longer independently apply notification routes, force-replace the active direct listener or globally render. Each eligible activation rereads the installation-local inbox. A selected route is persisted and rendered before its record is consumed.

The user also reported the active conversation jumping and erasing typed text. Code proof showed async callbacks globally rebuilt `#app`; the composer existed only in the DOM. The bounded repair adds one in-memory per-conversation composer state owner and one background chat projection path. Message/receipt/group/contact/attachment/Outbox updates replace only owned message/status/sidebar regions, leaving the textarea mounted. Structural rendering captures/restores draft, focus, selection and scroll; sign-out clears drafts. No plaintext draft database, Firebase/rules/backend, E2EE, receipt, Outbox or message-authority change. Version/cache: 1.1.21 / `1.1.21-activation-composer-owner`. FDA-NOTIFY-002 and FDA-COMPOSER-001 remain open until repeated iPhone/iPad acceptance.

### FIDUNIO 1.1.22 cold-route, immediate projection and newest-entry correction — 2026-09-10

1.1.21 device evidence: background iPhone/iPad repeatedly reached the direct chat but required ten seconds or more for the new message to appear; terminated iPhone could open Messages while terminated iPad behaved like background; and direct chats opened at the oldest message on both devices, including ordinary Messages-list entry. The route defect is a cold-start ordering race: a stale local conversation record can exist before cloud metadata reconciliation and was rejected/consumed without server revalidation. The latency defect is history-wide sequential e2ee:3 re-decryption plus cache/state and server Read waits before first paint. The oldest-row defect is 1.1.21 navigation scroll stored in the long-lived per-conversation composer map.

1.1.22 keeps the one `requestAppActivation()` and `render()` owners. Questionable local notification targets are resolved through one `firebase.js` server read; temporary failure keeps the route, and consumption requires the exact target composer mounted. The direct snapshot reuses already authenticated plaintext for immutable known message IDs, decrypts only new/unavailable rows, updates state, and requests composer-safe visible projection before IndexedDB durability and Read recovery. Composer memory now contains draft/focus/caret/height only. Scroll is captured for one same-chat render; intentional notification/list entry opens newest.

Files/resources touched: `app.js`, bounded `firebase.js` read API, version/cache identifiers, permanent tests/workflow and required release/architecture/defect documents. Screens/functionality requiring device retest: cold/warm PIN activation, Messages, exact direct conversation, latest-row placement, active composer, iPad sidebar, ordinary Settings/Profile/User Administration loading, chooser, receipts, offline replay and one attachment. Firebase backend/rules/config, notification payload/worker action, PIN/auth, E2EE format/key, receipt writer, Outbox, groups, attachments, disappearing content and Settings owners are unchanged. Version/cache: 1.1.22 / `1.1.22-notification-projection-owner`. Device acceptance remains open.

### FIDUNIO 1.1.22 repository/deployment evidence — 2026-09-10

Exact runtime commit `c2e98d802c5ea2851885f4f5bb2a08ef67af7f4c` is on `main`. Rebuild Baseline Security Gate `34494444866`, E2EE Recovery `34494444546`, E2EE Firebase Adapter `34494444308`, E2EE Rules Emulator `34494444767`, and GitHub Pages `34494429455` all completed SUCCESS. The live site serves `globalThis.FIDUNIO_RELEASE` version `1.1.22` and service-worker shell revision `1.1.22-notification-projection-owner`. Protected `firebase-config.js` remains SHA-256 `46b73e5194190f358b2b768c864f4c41e3b4b67388a7333be5b295fb85bd2209`; `config-firestore.js` remains absent. Repository/deployment acceptance is complete; FDA-NOTIFY-002/FDA-COMPOSER-001 product closure still requires the documented repeated real-device matrix.

### FIDUNIO 1.1.22 rejected; 1.1.23 deterministic-owner correction — 2026-09-10

Real-device acceptance rejects 1.1.22 despite green repository deployment. Background iPhone/iPad usually reached the direct chat but new-message visibility alternated between instant and delayed; terminated iPhone opened Messages; terminated iPad matched its background behavior; and both devices alternated between oldest/newest entry. Code inspection found the remaining exact races: a readiness reason can be stranded while the activation promise releases; callback transfer also issued a competing full-history read and queued every snapshot FIFO; and an intervening background render could replace an unresolved latest-entry animation frame with initial scroll zero.

1.1.23 keeps single ownership. The activation owner restarts synchronously if release finds queued reasons. One live Firestore listener transfers callback ownership without another `getDocs`; one active projection retains only the latest pending snapshot. Every central render advances a generation, and only that generation/exact mounted conversation may apply and clear the retained viewport intent. Latest entry survives intervening background projection. Runtime version/cache: 1.1.23 / `1.1.23-deterministic-activation-projection`. No diagnostic release or backend/rules/config, worker payload/display/click, PIN/auth, E2EE, receipt, Outbox, group, attachment, disappearing-content or Settings-owner change.

### FIDUNIO 1.1.23 repository/deployment evidence — 2026-09-10

Exact runtime tree `49be379988f7498d74db43a916438341bad12ef8` was pushed to `main` as `9fa9bb62ade01c5b6a3bcf01fc9f7fd6ccb6e57c`. Rebuild Baseline Security Gate `34501753123`, E2EE Firebase Adapter Gate `34501753059`, and GitHub Pages `34501752319` completed SUCCESS. Live Pages serves `globalThis.FIDUNIO_RELEASE` version `1.1.23`. Protected `firebase-config.js` remains SHA-256 `46b73e5194190f358b2b768c864f4c41e3b4b67388a7333be5b295fb85bd2209`; `config-firestore.js` remains absent. Repository/deployment acceptance is complete; notification, projection, viewport and composer product closure remains open until the repeated iPhone/iPad matrix passes.

### Approved next correction after FIDUNIO 1.1.23 device evidence — notification-priority semaphore

Real-device 1.1.23 evidence: iPhone background notification -> PIN -> exact direct chat displayed the new row instantly eight times, with one oldest-row occurrence; terminated iPhone now behaves the same and is a major cold-start improvement. Terminated iPad displayed instantly eight times. Background/warm iPad routes to the exact chat but new-row visibility remains inconsistent and mostly slow. This isolates the remaining latency to warm iPad stream recovery rather than general FCM delivery, notification routing, cold startup or the renderer itself.

Approved design for the next build: the installation-local pending notification record is a durable semaphore keyed by conversationId/messageId, not a global boolean. The service worker writes PENDING before notification display. PIN validation remains only the unlock authority. After PIN succeeds, the sole activation owner keeps the PIN transition mounted, changes its honest status to Opening message, and claims PENDING -> PROCESSING. The sole Firebase/message owner first accepts the exact message from the existing listener if already available; otherwise it performs one targeted server-authoritative read for that message without waiting for the suspended warm-iPad listener. Listener delivery and targeted delivery feed the same serialized owner; whichever supplies the ID first wins, and later delivery is idempotently deduplicated by messageId. There is no second listener, renderer or parallel state mutation.

Notification priority must be checked at safe boundaries ahead of history caching, general state persistence, Read-receipt recovery and Outbox reconciliation, so an old active snapshot cannot hold the notified message behind maintenance. The sole render owner decrypts/merges and mounts the exact conversation at the newest message while the unlock transition hides incomplete chat state. Clear PROCESSING -> CONSUMED only after the exact conversation, composer and notified message are visibly mounted. On fetch/decrypt/route/render failure or bounded timeout, never consume the semaphore: PIN success remains valid, the exact chat may show an honest Loading new message status, and the pending record remains retryable. With no pending notification semaphore, skip this priority path entirely. The existing listener remains active afterward for later messages, receipts, deletion and full authoritative reconciliation. Do not implement until the user explicitly resumes the build.

### FIDUNIO 1.1.24 keyed notification-priority implementation — 2026-09-10

The user explicitly resumed the build. The approved design is implemented as a frontend-only 1.1.24 candidate. The exact notification message uses one server document read and the existing per-conversation delivery owner; priority, listener snapshots, and post-display maintenance are serialized. Partial priority data merges by message ID and cannot purge history/Outbox. PIN stays on “Opening message…” during the bounded attempt. Route consumption now requires readiness plus the exact mounted message and composer; timeout leaves an honest loading state and durable retry. Permanent ownership/integration gates cover this contract. All local non-emulator tests, standalone workflow gates, and five emulator-backed rule suites pass. Push/workflows/Pages and real-device acceptance remain pending at this checkpoint.

### FIDUNIO 1.1.24 repository/deployment evidence — 2026-09-10

Exact runtime tree `9a3bcea96c726958c58f788980792f051921dabe` was pushed to `main` as `17210008b89da26ee137cbbd529bb222c09fc164` after design commit `e9aeaf412e70f1044482623c63a3d5bac5a44ad4`. Rebuild Baseline `34529867052`, E2EE Rules `34529867135`, E2EE Recovery `34529866986`, Firebase Adapter `34529867158`, and Pages `34529864925` completed SUCCESS. Live Pages serves version `1.1.24`, cache revision `1.1.24-notification-priority-semaphore`, and the cached delivery-owner module. Protected `firebase-config.js` remains SHA-256 `46b73e5194190f358b2b768c864f4c41e3b4b67388a7333be5b295fb85bd2209`; `config-firestore.js` remains absent. Repository/deployment acceptance is complete; repeated iPhone/iPad acceptance remains open.

### FIDUNIO 1.1.24 device-accepted checkpoint — 2026-09-10

The user tested every defined 1.1.24 notification scenario at least five times on both iPhone and iPad and reported that all behaved as expected. FDA-NOTIFY-002, the notification-related FDA-DM-001 projection/positioning scope, and FDA-COMPOSER-001 are closed for this release. Preserve 1.1.24 as the documented checkpoint/rollback baseline for subsequent work. This closeout is documentation-only and is intentionally left local for inclusion with the next requested build.

### Agreed deferred N6 group-notification rule — 2026-09-10

When the group-notification release is explicitly requested, derive recipients from the authoritative current active group-member list at notification-processing time, exclude the sender, and send the generic notification to enabled installations. Carry only opaque conversation/message routing identifiers. Do not add a separate joined-before-message notification check or snapshot message-time recipients for the initial implementation. Notification delivery is never access authority: after PIN, the existing group membership/history-entitlement, Firestore and E2EE owners decide whether the actual message can be opened. Stale, deleted or unauthorized routes fail closed without manufacturing a row. Reuse the 1.1.24 single activation/message/render ownership and priority-semaphore pattern. This decision is recorded locally only; combine it with the next requested release commit/push.

### FIDUNIO 1.1.25 password-change correction — 2026-09-11

Change Password was deterministically broken: `changeAccountPasswordWithE2EE` passed `{pin}` to the established rewrap contract requiring `{oldPin,newPin}`, so both were undefined and six-digit validation stopped the operation before Firebase changed the password. Version 1.1.25 passes the unchanged user PIN explicitly in both fields for forward rewrap and Firebase-failure rollback. The Change Password card owns its Current Password input; the Profile current-password field is now labeled only for email changes; successful change clears transient password/PIN inputs. Preserve the same E2EE identity/keyId, history and PIN. No Firebase rules/config, recovery, unlock, notification, message, receipt, Outbox, group, attachment, disappearing-content, Profile-save or User Administration authority changes. Permanent full-baseline coverage is required, followed by `main`/Pages evidence and real-device acceptance.

### FIDUNIO 1.1.26 local revision repair — 2026-09-11

The 1.1.25 password mutation succeeded in Firebase and in the cloud E2EE wrapper, but rewrap incremented the authoritative revision without refreshing the device-local identity row. Login therefore succeeded and then produced “Secure messaging is not available on this device.” The correction saves the runtime identity after successful rewrap and rollback. For an already-affected installation, only an existing local row with the same UID/keyId and stale revision may use the established new-password plus existing-PIN remote unlock to resynchronize. Missing or different-key installations remain rejected. Do not advise Forgot Password or rejoin for this case; no key or message was lost.

### Deferred administrator-authorized account recovery — 2026-09-11

The agreed future design belongs in User Administration, but the administrator only authorizes a recovery opportunity and must never perform the user's cryptographic recovery. A selected-user action creates a UID-bound, short-lived (target 30–60 minutes), single-use and revocable recovery invitation with initiating-admin and lifecycle audit timestamps. The user verifies the Firebase email and privately chooses the new password and six-digit FIDUNIO PIN. The established recovery owner restores the same account key and history. Administrators may see only recovery status and audit metadata—never passwords, PINs, private keys, recovery secrets, decrypted messages or attachments. Recovery cannot alter roles, group membership, or message/history entitlement. Successful use consumes the invitation; expired, revoked, reused, wrong-user and incomplete attempts fail closed. Keep this distinct from matching-key installation repair. Implementation requires bounded User Administration UI, secure callable backend, user recovery screen, authorization/audit storage, expiry/revocation, permanent unit/integration/emulator gates and repeated iPhone/iPad acceptance. This is documentation-only and must not be built until explicitly requested.

### FIDUNIO 1.1.27 Forgot Password lifecycle — 2026-09-11

Investigation proved the old Forgot Password control only sent the Firebase email. It did not transition to E2EE recovery: a saved local key could hide the wrapper/password mismatch, while a missing-local installation was rejected before reaching the Settings recovery control. Version 1.1.27 records a non-secret email-bound 24-hour local handoff after the reset-email request succeeds. The next successful sign-in stays at the authentication gate, requests the existing PIN, and uses the established ten-minute server recovery session to restore the same private key, rewrap it under the new password, save the current local revision, and consume the handoff only after success. Authenticated missing-local installations receive the same recovery surface. Wrong PIN and all server expiry/attempt/hold/UID/keyId/revision checks fail closed. This user self-recovery does not implement or change the separately deferred administrator-authorized recovery feature.

### Deferred TODO item 2 encrypted vault reconnection design — 2026-09-11

The user selected both a same-device checkpoint and portable encrypted whole-account vault. Existing `fidunio-account-vault-v3` is only a foundation: it snapshots encrypted app/history/Outbox state and identity metadata, but depends on a separate non-exportable same-origin local key and cannot survive complete origin deletion as a self-contained file. The future `.fidunio` format must use a wrapped Vault Backup Key and require matching Firebase UID + existing PIN + server recovery authority; possession or PIN alone is insufficient. Restore is a quarantine-first, single storage/startup-owner transaction: validate integrity/schema/account/key, recover the same identity, decrypt candidate, reject unsafe revision replacement, refresh cloud authority, reconcile every Outbox ID, recreate device-bound registrations, atomically activate and retain deterministic rollback. Never restore auth sessions, FCM tokens, biometrics, temporary notification routes or stale access authority. Icon-only removal may reconnect the existing internal vault; complete origin loss uses the exported file. Full security, corruption, rollback, multi-account/device and iPhone/iPad/Fire 8 acceptance requirements are attached to TODO item 2. Documentation-only; do not implement until explicitly requested.

### FIDUNIO 1.1.28 N6 group notifications — 2026-09-11

N6 derives recipients from current authoritative group membership, excludes the sender, and sends opaque data-only payloads to enabled installations in batches of at most 500. After PIN, the single activation owner revalidates current membership and routes an exact server message read through the sole group decrypt/projection stream ahead of coalesced listener work; the central render owner alone reveals the group chat. Missing or unauthorized targets fail closed, transient failures remain pending, and notification metadata never grants access or owns message/receipt state. Implementation commit `c273f0bfc0275c612fb91c916da0f4d08109dcaa` and pinned one-function deployment handoff `gn.txt` are on `main`; all implementation and handoff workflows plus live Pages verification passed. Live backend deployment and repeated iPhone/iPad device acceptance remain pending.

### FIDUNIO 1.1.28 N6 group notification implementation — 2026-09-11

The user explicitly requested the recorded N6 build. `notifyGroupMessageCreatedV1` derives recipients from current authoritative group `memberUids`, excludes the sender, and uses the shared enabled-installation/stale-token owner with 500-token FCM batches. Payloads remain data-only and opaque. The worker/inbox now accepts `group-message`; after PIN, the one activation owner revalidates current membership and submits the exact document to the existing group decrypt/projection owner through a keyed priority queue. Priority projection precedes cache/persistence/receipt work and only central render reveals chat. Unauthorized or missing routes fail closed; transient failures remain pending. Direct 1.1.24 behavior, E2EE formats, Outbox, rules and protected configuration remain unchanged. Full workflows, Pages, pinned live Function deployment and device acceptance are still required.
FIDUNIO 1.1.29 GROUP NOTIFICATION SINGLE-STREAM CORRECTION (2026-09-11): After successful live `gn.txt` deployment, iPad group-notification acceptance produced the correct notification, PIN route, group and exact message, but 2 of 4 retrievals displayed `Firebase connection problem: Missing or insufficient permissions.` Source tracing found repeated foreground/notification activation could replace the same active group stream because the group path lacked the direct path's conversation-identity reuse guard. A replaced stream could then continue asynchronous history/receipt maintenance and surface a stale Firestore error after successful projection. Version 1.1.29 retains one active group stream per conversation, settles late priority work when a stream closes, and blocks all closed-stream projection/history/receipt/error callbacks. No Firestore rules, backend Function, FCM payload, group E2EE, membership, Outbox, receipt authority or protected Firebase configuration changes. Focused permanent gates pass; full workflows, Pages and repeated iPhone/iPad acceptance remain required.

FIDUNIO 1.1.29 DEVICE ACCEPTANCE (2026-09-11): The user reports that every cold and warm group-notification test passed on both iPad and iPhone. The intermittent permission banner did not recur. N6 group-notification retrieval is device accepted at this checkpoint.

FIDUNIO 1.1.30 OPTIMISTIC OUTGOING PROJECTION CORRECTION (2026-09-11): After N6 acceptance, the user reported that a newly sent message disappeared from the sender's conversation until Sent confirmation, instead of remaining visible with Sending. Inspection confirmed the established code still called render before asynchronous Outbox work, but the permanent LTE gate asserted source ordering only and did not model an intervening Firestore listener projection. A new memory-only optimistic projection owner reserves the exact text or attachment row before first render. Direct and group snapshot projections retain the row until the exact authoritative message ID appears; the server row then wins. Explicit purge/delete and sign-out release/reset reservations. This preserves one app render owner and does not create a second Outbox, transport, Firebase, E2EE, receipt, notification or membership authority. A behavioral gate covers empty and unrelated authoritative snapshots, confirmation, deletion and hidden-message non-resurrection. Version/cache are 1.1.30 / `1.1.30-optimistic-outgoing-projection`. The full local non-emulator baseline, all five Firestore emulator suites, Functions notification/delete tests, syntax, protected-config and diff checks pass; main publication, Pages and device acceptance remain required.
