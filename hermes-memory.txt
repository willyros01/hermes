HERMES PROJECT — FIDUNIO PRODUCT
CUMULATIVE PROJECT MEMORY
Protected rollback baseline: FIDUNIO 0.8.1.16 at commit 2a6380e26ce969fc95f0a38e29517e48b09b781b
Current validated development baseline: FIDUNIO 0.9.0.4
Current live test build on main: FIDUNIO 0.9.3.3
Development branches retained: release/0.9.0 and release/0.9.1
Internal project/repo name: Hermes
Public app/product name: FIDUNIO — Private Messaging

LOCKED CONVENTIONS
- Hermes = internal project/repository/release ZIP naming; FIDUNIO = public app/product.
- Keep release files flat in repository root.
- Keep exactly one cumulative root hermes-memory.txt and one reusable root hermes-setup.txt; no version-numbered memory/setup TXT.
- firebase-config.js and config-firestore.js must NEVER be regenerated, overwritten, or included in update/release ZIPs.
- version.js is the single authoritative runtime version source; every test build gets a distinct visible version.
- Preserve winged-messenger artwork, navy/teal/gray palette, no gold.
- Preserve iPhone single-pane and adaptive iPad two-pane behavior; responsive by viewport width.

STABLE BASELINES
- 0.6.5 local-first/offline PASS; 0.7.3 direct E2EE transport/reconnect PASS; 0.8.0 device identity PASS; 0.8.1 contact key verification PASS.
- UI 0.8.1.9 plus Group Info tablet fix PASS.
- 0.8.1.16 receipt/re-entry PASS and protected rollback.
- 0.9.0.2 per-device E2EE two-device + receipt/re-entry + offline Outbox/reconnect PASS.
- 0.9.0.4 full three-device bidirectional per-device E2EE fan-out PASS and remains validated development baseline.
- Preserve 0.9.0.4 messaging, receipt, Outbox and per-device fan-out unchanged.

0.9.1 GROUP FOUNDATION
- Separate /groups schema/rules and real-user group metadata UI are in place. Group message writes remain disabled until group E2EE/key rotation is implemented.
- 0.9.1.1 real New Group user list PASS. 0.9.1.2 prototype/mock conversation cleanup PASS.

0.9.2 INVITE / PROFILE / SETTINGS
- Invite-only admission, Owner bootstrap, Forgot Password, Change Password and editable profile are implemented.
- Firebase Account and Device Identity are collapsed by default.
- Profile display-name propagation uses authoritative /users/{peerUid}.displayName listeners, not stale memberNames.
- Visible Sign Out is on the main Messages/chat screen.
- Prototype connectivity card removed while Outbox/reconnect remains intact.
- Explicit Sign In and successful invited-account creation bypass the immediately-following local PIN/biometric prototype screen once.
- 0.9.2.8 made the auth gate tablet-width aware.
- 0.9.2.9 moved Privacy & Access and Appearance to the right side but CSS grid row coupling still produced large vertical gaps.

0.9.3 USER ADMIN + SETTINGS COLUMN MODEL
- Settings landscape uses true independent left/right column containers rather than one coupled two-column CSS grid. About stays full-width last. Future unclassified Settings cards remain visible rather than being discarded.
- 0.9.3.1 moved User Administration to the left Settings column under Profile. Right side keeps Appearance, Text Size, Data, Device Identity, Privacy & Access and Invitations.
- Added account lifecycle administration and account-guard enforcement. Published Firestore rules are required and user confirmed the rules from main were published.
- User Administration supports Suspend, Restore, Deactivate, expiration Never/7/30/90 days and pending invitation revoke. Owner is protected; Admin cannot manage Owner or another Admin.
- 0.9.3.2 replaces large per-user management cards with a compact scalable user table. Columns: User, Role, Status, Expires, Actions. Each manageable user has a small ••• menu for Suspend/Restore, Deactivate and expiration changes. Owner/current/protected rows stay compact and non-actionable. Pending invitations remain in a compact list below the table.
- User explicitly preferred this compact table because more users will be easier to maintain.
- 0.9.3.3 replaces the inline invitation role/expiry selectors with a dedicated Create Invitation modal. The modal provides working User/Admin and 1/7/30-day selectors, Cancel and Create Invitation actions, and the one-time invitation explanation. Successful creation returns the generated link to the existing invitation result card with Copy Link and Share support. This specifically fixes the iPad case where the inline selectors appeared dead.
- firebase-config.js was not touched.

NEXT LIVE TEST
1. Refresh/reopen and confirm FIDUNIO v0.9.3.3.
2. Open Settings → Invitations and tap Create Invitation.
3. Confirm the modal opens, User/Admin selector changes, expiration selector changes, Cancel closes it, and Create Invitation generates the link.
4. After invitation modal passes, next requested feature is real local PIN + biometric/passkey inactivity lock, including configurable idle timeout and Lock Now. Successful explicit account Sign In / invited-account creation must continue to bypass an immediate local re-lock once.

SECURITY / ADMIN PLAN
- Account lifecycle: Active → Suspended → Deactivated; avoid destructive hard-delete by default.
- True Firebase Auth disable/delete still requires a privileged backend/Admin SDK rather than client-only GitHub Pages.
- Next security work requested by user: local inactivity PIN/passkey/biometric lock, then audit/security events and new-device alerts/device proliferation checks.

IMPORTANT TECHNICAL DETAILS
- app.js imports firebase.js; firebase.js imports protected firebase-config.js.
- index.html loads version.js then bootstrap.js. bootstrap loads settings-polish.js, auth-unlock-bridge.js, account-guard.js, auth-ui.js, profile-sync.js, main-screen-polish.js, admin-ui.js and invite-modal.js.
- IndexedDB fidunio-local v2; encrypted local state/Outbox/history. Firebase data is never service-worker cached.
- E2EE v2 uses per-device P-256 ECDH + HKDF SHA-256 + AES-GCM envelopes; e2ee:1 legacy history compatibility retained.
- Account-level compatibility trust is not final multi-device trust design; future device-level approval/safety numbers remain required.