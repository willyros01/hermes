HERMES PROJECT — FIDUNIO PRODUCT
CUMULATIVE PROJECT MEMORY
Current pre-0.9.0 validated baseline: FIDUNIO 0.8.1.16
Internal project/repo name: Hermes
Public app/product name: FIDUNIO — Private Messaging

LOCKED CONVENTIONS
- Hermes = internal project/repository/release ZIP naming; FIDUNIO = public app/product.
- Keep release files flat in repository root.
- Keep exactly one cumulative root hermes-memory.txt and one reusable root hermes-setup.txt; no version-numbered memory/setup TXT.
- firebase-config.js and config-firestore.js must NEVER be regenerated, overwritten, or included in update/release ZIPs.
- version.js is the single authoritative runtime version source.
- Every test build gets a distinct visible version.
- Preserve winged-messenger artwork, navy/teal/gray palette, no gold.
- Preserve iPhone single-pane and adaptive iPad two-pane behavior; responsive by viewport width.

ARCHITECTURE / STABLE BASELINES
- JavaScript PWA, Firebase Auth + Firestore, Web Crypto, encrypted IndexedDB local history/Outbox, Service Worker.
- 0.6.5 local-first/offline baseline: PASS.
- 0.7.3 direct E2EE transport/reconnect baseline: PASS.
- 0.8.0 device identity baseline: PASS.
- 0.8.1 contact key verification baseline: PASS.
- UI 0.8.1.9 plus Group Info tablet fix: PASS.
- 0.8.1.16 receipt/re-entry baseline: PASS.

FIREBASE
- Project fidunio-fef13; Auth Email/Password; Firestore Standard (default), nam5; authorized domain willyros01.github.io.
- Account A on iPad, Account B on iPhone; different Firebase accounts.
- Do not ask user to recreate accounts/re-paste UID unless restoration truly fails.

IPAD SENT → READ DEFECT HISTORY
Original reproducible defect:
- iPad sends to iPhone.
- If iPhone conversation is already open, receipt can advance to Read on iPad.
- If iPhone leaves conversation, iPad sends another message, then iPhone re-enters and sees it, iPad could remain Sent.

0.8.1.10 FAILED:
- 2-second server polling caused constant refresh and broke sending. Rejected permanently; do not reintroduce polling.

0.8.1.11:
- Polling rollback restored normal messaging; receipt defect remained.

0.8.1.12 FAILED:
- Serialized async Firestore snapshot processing. Initial PASS was false positive; repeated testing reproduced defect.

0.8.1.13 FAILED:
- Removed ordinary two-pane force:true listener restarts at runtime. Defect remained.

0.8.1.14 DIAGNOSTIC PARTIAL PASS:
- Added receipt metadata fast-path before peer-key refresh/decryption.
- First message with iPhone conversation open changed to Read on iPad.
- Message sent while iPhone was outside the chat still remained Sent after re-entry.

0.8.1.15 FAILED:
- Tried marking already-known incoming local rows Read from renderChat on entry.
- Same re-entry failure remained.

0.8.1.16 PASS — VALIDATED BASELINE:
- Restored established source listener behavior and removed the failed v13 wide-layout runtime rewrite.
- Kept the v14 outgoing receipt metadata fast-path.
- On narrow/mobile conversation re-entry, the existing force:true message subscription reattachment obtains a fresh server-backed snapshot; the normal active-chat callback marks incoming unread rows Read.
- User repeated the exact failing sequence and confirmed it now works: open iPhone conversation/read first message; leave conversation; send second message from iPad; re-enter iPhone conversation; untouched iPad advances Sent → Read.
- No polling, Firestore Rules, E2EE, Outbox, or layout changes.
- Treat FIDUNIO 0.8.1.16 as the pre-0.9.0 rollback/baseline release.

CURRENT STATUS
Validated baseline on main: FIDUNIO 0.8.1.16.
Validated: encrypted history, cloud sending, offline/local-first foundation, device identity, contact key verification, iPad two-pane layout, Group Info tablet layout, live Sent → Read including iPhone leave/re-enter sequence.
Next major work may proceed toward 0.9.0 true per-device E2EE while preserving 0.8.1.16 behavior.

0.9.0 TRUE PER-DEVICE E2EE ROADMAP
1. Read recipient registered device set.
2. Encrypt message key separately for each authorized recipient device.
3. Add per-device recipient envelopes/fan-out.
4. Preserve validated tablet UI and 0.7.3 transport/offline/reconnect foundation plus 0.8.1.16 receipt behavior.
5. Regression/security test.
6. Verified device linking/management and stronger device-change approval.
7. Later safety numbers/QR, forward secrecy/Double Ratchet or vetted equivalent, group E2EE/key rotation/history enforcement, App Check/rules audit, quota/billing, push, passkeys.
- Do not use homegrown crypto beyond careful vetted Web Crypto primitives/protocol design.

IMPORTANT TECHNICAL DETAILS
- app.js imports firebase.js; firebase.js imports firebase-config.js.
- index.html loads version.js before app.js; service-worker.js imports version.js.
- IndexedDB DB fidunio-local v2; stores meta, outbox, history.
- E2EE keypair meta e2ee-device-keypair-v1; device identity e2ee-device-identity-v1.
- Firebase data is never service-worker cached.
- navigator.onLine is not proof of actual Internet reachability.

GROUP REQUIREMENTS
- Group UX similar to direct chat; sender shown for incoming group messages.
- New Message → New Group → select people → name → create.
- Group Info members/add/remove/rename/mute/search/shared media/files/disappearing/security/leave.
- Creator Owner/Admin; new member sees from join time by default; admin may grant earlier history.
- Production enforcement cryptographic; rotate group keys on membership changes.

FIRE HD 8
- Useful for Android-derived PWA/Web Crypto/IndexedDB/Firebase transport testing, but not full Google-certified Android target.
- Until true per-device recipient fan-out exists, do not sign Fire 8 into either working iPad/iPhone test account unless intentionally testing migration/device behavior.
